{
  "name": "quasi-workflow-build",
  "private": true,
  "type": "module",
  "scripts": {
    "build:workflows": "node scripts/build-workflows.mjs",
    "check:workflows": "node scripts/build-workflows.mjs --check && tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "typescript": "^5.9.3"
  }
}
