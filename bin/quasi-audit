#!/usr/bin/env bash
# quasi-audit — agent-facing vault typecheck wrapper.
#
# Usage:
#   quasi-audit [--path FILE_OR_DIR]
#
# Always runs mechanical autofix, typecheck, residual issue classification,
# then emits JSON to stdout. This is the entry point audit-agent uses.

set -e

SCRIPT_PATH="$(realpath "${BASH_SOURCE[0]}")"
PLUGIN_ROOT="$(dirname "$(dirname "$SCRIPT_PATH")")"
DATA_DIR="${CLAUDE_PLUGIN_DATA:-$HOME/.cache/quasi}"
PYTHON="$DATA_DIR/.venv/bin/python"

[ -x "$PYTHON" ] || "$PLUGIN_ROOT/scripts/bootstrap-venv.sh"

exec "$PYTHON" "$PLUGIN_ROOT/scripts/audit/audit.py" "$@"
