// GENERATED by scripts/build-workflows.mjs. DO NOT EDIT.
// Source of truth: scripts/workflows/author.entry.mts and its ESM imports.

export const meta = {
  "name": "Quasi Author",
  "description": "Compose representative Paper and Book workflows for one Author.",
  "phases": [
    {
      "title": "Search"
    },
    {
      "title": "Analyse"
    },
    {
      "title": "Synthesise"
    },
    {
      "title": "Audit"
    }
  ]
}

"use strict";var __quasiWorkflow=(()=>{var __defProp=Object.defineProperty;var __getOwnPropDesc=Object.getOwnPropertyDescriptor;var __getOwnPropNames=Object.getOwnPropertyNames;var __hasOwnProp=Object.prototype.hasOwnProperty;var __export=(target,all)=>{for(var name in all)__defProp(target,name,{get:all[name],enumerable:true})};var __copyProps=(to,from,except,desc)=>{if(from&&typeof from==="object"||typeof from==="function"){for(let key of __getOwnPropNames(from))if(!__hasOwnProp.call(to,key)&&key!==except)__defProp(to,key,{get:()=>from[key],enumerable:!(desc=__getOwnPropDesc(from,key))||desc.enumerable})}return to};var __toCommonJS=mod=>__copyProps(__defProp({},"__esModule",{value:true}),mod);var author_entry_exports={};__export(author_entry_exports,{materialKind:()=>materialKind,run:()=>run,workflowMeta:()=>workflowMeta});var CONTROL_CHARS=new RegExp("[\\u0000-\\u001f\\u007f-\\u009f]");var validText=(value,min,max)=>typeof value==="string"&&value===value.trim()&&value.length>=min&&value.length<=max&&!CONTROL_CHARS.test(value);var exactKeys=(value,keys)=>!!(value&&typeof value==="object"&&!Array.isArray(value)&&Object.keys(value).length===keys.length&&keys.every(key=>Object.prototype.hasOwnProperty.call(value,key)));function sameClosedValue(left,right){if(Array.isArray(left)||Array.isArray(right))return Array.isArray(left)&&Array.isArray(right)&&left.length===right.length&&left.every((value,index)=>sameClosedValue(value,right[index]));if(left&&right&&typeof left==="object"&&typeof right==="object"){const leftKeys=Object.keys(left);const rightKeys=Object.keys(right);return leftKeys.length===rightKeys.length&&leftKeys.every(key=>Object.prototype.hasOwnProperty.call(right,key)&&sameClosedValue(left[key],right[key]))}return Object.is(left,right)}var MATERIAL_SLUG=/^[a-z0-9][a-z0-9-]{0,79}$/;var LANGUAGE_TAG=/^[A-Za-z]{2,8}(?:-[A-Za-z0-9]{2,8}){0,3}$/;var isRecord=value=>!!value&&typeof value==="object"&&!Array.isArray(value);var exactKeys2=(value,required,optional=[])=>{const allowed=new Set([...required,...optional]);return required.every(key=>Object.hasOwn(value,key))&&Object.keys(value).every(key=>allowed.has(key))};var validString=(value,minLength,maxLength)=>typeof value==="string"&&value.length>=minLength&&value.length<=maxLength;var validNullableString=(value,maxLength)=>value===null||validString(value,0,maxLength);var validAuthors=value=>Array.isArray(value)&&value.length>=1&&value.length<=32&&value.every(item=>validString(item,1,200));var validMaterialSlug=value=>typeof value==="string"&&MATERIAL_SLUG.test(value);function normalizeLanguage(value){if(typeof value!=="string"||!LANGUAGE_TAG.test(value))return null;const normalized=value.split("-").map((part,index)=>{if(index===0)return part.toLowerCase();if(/^[A-Za-z]{2}$/.test(part))return part.toUpperCase();return part.toLowerCase()}).join("-");return normalized==="zh-CN"?"zh":normalized}var isArtifactObservation=value=>isRecord(value)&&exactKeys2(value,["path","present","usable"])&&typeof value.path==="string"&&typeof value.present==="boolean"&&typeof value.usable==="boolean"&&(!value.usable||value.present);var isArtifactList=value=>Array.isArray(value)&&value.every(isArtifactObservation);var parseStatusEnvelope=(value,expectedKind)=>{if(!isRecord(value)||!exactKeys2(value,["schema_version","kind","slug","identity","facts"])||value.schema_version!=="quasi.status/0.2"||value.kind!==expectedKind||!validMaterialSlug(value.slug)||value.identity!==null&&!isRecord(value.identity)||!isRecord(value.facts))return null;return value};var parseObservationRoute=value=>{if(!isRecord(value)||!validMaterialSlug(value.slug))return null;if(["paper","book","talk","webpage","archive","topic"].includes(value.kind)&&exactKeys2(value,["kind","slug"]))return value;if(value.kind==="translation"&&exactKeys2(value,["kind","slug","target_language"])){const target=normalizeLanguage(value.target_language);if(target!==null&&target===value.target_language)return value}return null};var observationKey=route=>route.kind==="translation"?`translation:paper:${route.slug}:${route.target_language}`:`${route.kind}:${route.slug}`;var sparseObservations=entries=>{if(!Array.isArray(entries))return null;const observations=new Map;for(const rawEntry of entries){if(!isRecord(rawEntry)||!exactKeys2(rawEntry,["route","observation"]))return null;const route=parseObservationRoute(rawEntry.route);if(route===null)return null;const observation=parseStatusEnvelope(rawEntry.observation,route.kind);const observedTarget=route.kind==="translation"&&observation!==null?observation.facts.target_language:null;if(observation===null||observation.slug!==route.slug||route.kind==="translation"&&observedTarget!==route.target_language)return null;const key=observationKey(route);if(observations.has(key))return null;observations.set(key,observation)}return observations};var parseOptions=value=>isRecord(value)?value:null;var parseUserDecision=value=>{if(!isRecord(value)||!exactKeys2(value,["material_key","operation","value"])||!validString(value.material_key,1,512)||!validString(value.operation,1,200))return null;return value};var decisionForOperation=(decision,materialKey,operation,durableOutputComplete)=>!durableOutputComplete&&decision?.material_key===materialKey&&decision.operation===operation?decision.value:null;var parseLeafRunEnvelope=raw=>{if(!isRecord(raw)||!exactKeys2(raw,["seed","observation","options"],["userDecision"]))return null;const options=parseOptions(raw.options);const userDecision=Object.hasOwn(raw,"userDecision")?parseUserDecision(raw.userDecision):null;if(options===null||Object.hasOwn(raw,"userDecision")&&userDecision===null)return null;return{seed:raw.seed,observation:raw.observation,options,userDecision}};var requestedLeafSlug=raw=>{if(!isRecord(raw)||!isRecord(raw.seed))return null;if(raw.seed.state==="provisional"&&validMaterialSlug(raw.seed.requested_slug))return raw.seed.requested_slug;if(raw.seed.state==="canonical"&&validMaterialSlug(raw.seed.material_slug))return raw.seed.material_slug;return null};var MATERIAL_RESULT_VERSION="quasi.material.result/0.1";var materialBase=seed=>({schema_version:MATERIAL_RESULT_VERSION,material:{requested:seed.material.requested,canonical:seed.material.canonical}});var completeMaterialResult=(seed,artifacts,next)=>({...materialBase(seed),terminal:"complete",issue:null,artifacts,next});var needsInputMaterialResult=(seed,issue,gate,resumeSeed4)=>({...materialBase(seed),terminal:"needs_input",issue,gate,resume_seed:resumeSeed4});var needsObservationMaterialResult=(seed,routes,resumeSeed4)=>({...materialBase(seed),terminal:"needs_observation",issue:null,routes,resume_seed:resumeSeed4});var higherOrderNeedsInputMaterialResult=(seed,issue,route,gate,routes,resumeSeed4)=>({...materialBase(seed),terminal:"needs_input",issue,gate:{kind:"child",route,gate},routes,resume_seed:resumeSeed4});var stoppedMaterialResult=(seed,terminal,issue)=>({...materialBase(seed),terminal,issue});var blockedMaterialResult=(seed,issue)=>stoppedMaterialResult(seed,"blocked",issue);var invalidMaterialInputResult=requested=>stoppedMaterialResult({material:{requested,canonical:null}},"blocked",{code:"material.invalid_input",operation:null,summary:"Material Workflow input is invalid.",retryable:false,observation_request:null});var BOOK_TEMP_PATH=/^\.quasi\/temp\/downloads\/[A-Za-z0-9][A-Za-z0-9._-]{0,220}\.(?:epub|pdf)$/;var YEAR_SCHEMA={type:"integer",minimum:1500,maximum:2030};var NULLABLE_YEAR_SCHEMA={type:["integer","null"],minimum:1500,maximum:2030};var BOOK_YEAR_EVIDENCE_SCHEMA={type:"object",additionalProperties:false,required:["slug_year","source_years","pdf_signals","recommended_year","recommendation_reason","verdict"],properties:{slug_year:YEAR_SCHEMA,source_years:{type:"object",additionalProperties:YEAR_SCHEMA,maxProperties:64},pdf_signals:{type:"object",additionalProperties:false,required:["first_published","copyright_year","original_year","other_years"],properties:{first_published:NULLABLE_YEAR_SCHEMA,copyright_year:NULLABLE_YEAR_SCHEMA,original_year:NULLABLE_YEAR_SCHEMA,other_years:{type:"array",maxItems:64,items:YEAR_SCHEMA}}},recommended_year:NULLABLE_YEAR_SCHEMA,recommendation_reason:{type:"string",minLength:1,maxLength:4e3},verdict:{type:"string",enum:["MATCH","MISMATCH","AMBIGUOUS"]}}};var bookYear=value=>Number.isInteger(value)&&value>=1500&&value<=2030;var nullableYear=value=>value===null||bookYear(value);var validBookTempPath=value=>typeof value==="string"&&BOOK_TEMP_PATH.test(value);function parseBookYearEvidence(value){const evidence=value;if(!exactKeys(evidence,["slug_year","source_years","pdf_signals","recommended_year","recommendation_reason","verdict"])||!bookYear(evidence.slug_year)||!evidence.source_years||typeof evidence.source_years!=="object"||Array.isArray(evidence.source_years)||Object.keys(evidence.source_years).length>64||Object.entries(evidence.source_years).some(([source,year])=>!validText(source,1,200)||!bookYear(year))||!exactKeys(evidence.pdf_signals,["first_published","copyright_year","original_year","other_years"])||!nullableYear(evidence.pdf_signals.first_published)||!nullableYear(evidence.pdf_signals.copyright_year)||!nullableYear(evidence.pdf_signals.original_year)||!Array.isArray(evidence.pdf_signals.other_years)||evidence.pdf_signals.other_years.length>64||evidence.pdf_signals.other_years.some(year=>!bookYear(year))||!nullableYear(evidence.recommended_year)||!validText(evidence.recommendation_reason,1,4e3)||!["MATCH","MISMATCH","AMBIGUOUS"].includes(evidence.verdict)||evidence.verdict==="MATCH"&&evidence.recommended_year!==evidence.slug_year||evidence.verdict==="MISMATCH"&&(evidence.recommended_year===null||evidence.recommended_year===evidence.slug_year)||evidence.verdict==="AMBIGUOUS"&&evidence.recommended_year!==null)return null;return evidence}var exactEnvelopeKeys=exactKeys2;var SHA256=/^[0-9a-f]{64}$/;var nonnegativeInteger=value=>Number.isInteger(value)&&value>=0;var parseProfile=(value,kind)=>{if(!isRecord(value)||!exactEnvelopeKeys(value,["schema_version","language","text_extractor","engine_order","chunk_pages","name","validation_policy"])||value.schema_version!=="quasi.ocr.profile/0.2"||value.language!=="chi_sim+eng"||value.text_extractor!=="pymupdf"||!Array.isArray(value.engine_order)||value.validation_policy!==(kind==="paper"?"paper-text-v1":"book-pdf-v1"))return null;const ds=value.name==="dsocr2-text"&&value.chunk_pages===16&&value.engine_order.length===2&&value.engine_order[0]==="dsocr2"&&value.engine_order[1]==="tesseract";const tesseract=value.name==="tesseract-text"&&value.chunk_pages===32&&value.engine_order.length===1&&value.engine_order[0]==="tesseract";return ds||tesseract?value:null};var validFileBase=(value,expectedPath)=>{if(value.path!==expectedPath||typeof value.exists!=="boolean"||![true,false,null].includes(value.regular)||!nonnegativeInteger(value.size)||!(value.sha256===null||typeof value.sha256==="string"&&SHA256.test(value.sha256)))return false;if(!value.exists)return value.regular===null&&value.sha256===null&&value.size===0;if(value.regular===false)return value.sha256===null&&value.size===0;return value.regular===true&&typeof value.sha256==="string"};var parseFile=(value,expectedPath)=>isRecord(value)&&exactEnvelopeKeys(value,["path","exists","regular","sha256","size"])&&validFileBase(value,expectedPath)?value:null;var parsePdf=(value,expectedPath)=>isRecord(value)&&exactEnvelopeKeys(value,["path","exists","regular","sha256","size","pages"])&&validFileBase(value,expectedPath)&&nonnegativeInteger(value.pages)&&(value.regular===true||value.pages===0)?value:null;var parseText=(value,expectedPath)=>{if(!isRecord(value)||!exactEnvelopeKeys(value,["path","exists","regular","sha256","size","utf8","chars","non_whitespace_chars"])||!validFileBase(value,expectedPath)||![true,false,null].includes(value.utf8)||!nonnegativeInteger(value.chars)||!nonnegativeInteger(value.non_whitespace_chars)||value.non_whitespace_chars>value.chars)return null;if(value.regular!==true)return value.utf8===null&&value.chars===0&&value.non_whitespace_chars===0?value:null;return value};var parseProgress=(value,sourcePages,profile,workRoot)=>{if(!isRecord(value)||!exactEnvelopeKeys(value,["completed_pages","total_pages","next_page","ranges"])||!nonnegativeInteger(value.completed_pages)||value.total_pages!==sourcePages||!Array.isArray(value.ranges)||value.next_page!==(value.completed_pages<sourcePages?value.completed_pages+1:null))return null;let cursor=1;for(const range of value.ranges){const expectedEnd=Math.min(cursor+profile.chunk_pages-1,sourcePages);if(!isRecord(range)||!exactEnvelopeKeys(range,["start_page","end_page","engine","path","sha256","pages"])||range.start_page!==cursor||range.end_page!==expectedEnd||!profile.engine_order.includes(range.engine)||range.pages!==range.end_page-cursor+1||range.path!==`${workRoot}/parts/part-${String(cursor).padStart(6,"0")}-${String(expectedEnd).padStart(6,"0")}.${String(range.engine)}.pdf`||typeof range.sha256!=="string"||!SHA256.test(range.sha256))return null;cursor=range.end_page+1}return cursor-1===value.completed_pages?value:null};var parseOcrGenerationObservation=(value,expected)=>{if(!isRecord(value)||!exactEnvelopeKeys(value,["state","material_key","kind","slug","generation_key","profile","config_fingerprint","source","paths","progress","manifest","recovery_pdf","normalized_text","failure"])||!["missing","in_progress","committed","invalid","unknown"].includes(value.state)||value.material_key!==`${expected.kind}:${expected.slug}`||value.kind!==expected.kind||value.slug!==expected.slug||typeof value.generation_key!=="string"||!SHA256.test(value.generation_key)||typeof value.config_fingerprint!=="string"||!SHA256.test(value.config_fingerprint)||!(value.failure===null||validString(value.failure,1,200))||!isRecord(value.source)||!exactEnvelopeKeys(value.source,["path","sha256","size","pages"])||value.source.path!==expected.source.path||value.source.sha256!==expected.source.sha256||value.source.size!==expected.source.size||!nonnegativeInteger(value.source.pages))return null;const profile=parseProfile(value.profile,expected.kind);if(profile===null)return null;const generation=value.generation_key;const materialRoot=`processing/${expected.kind==="paper"?"papers":"chapters"}/${expected.slug}`;const workRoot=`${materialRoot}/.ocr-work/${generation}`;const generationRoot=`${materialRoot}/ocr-generations/${generation}`;const expectedPaths={lock:`${materialRoot}/.ocr-generation.lock`,work_dir:workRoot,progress:`${workRoot}/ocr.progress.json`,generation_dir:generationRoot,manifest:`${generationRoot}/manifest.json`,pdf:`${generationRoot}/ocr.pdf`,text:`${generationRoot}/ocr.txt`};if(!isRecord(value.paths)||!exactEnvelopeKeys(value.paths,Object.keys(expectedPaths))||Object.entries(expectedPaths).some(([key,path])=>value.paths[key]!==path))return null;const manifest=parseFile(value.manifest,expectedPaths.manifest);const pdf=parsePdf(value.recovery_pdf,expectedPaths.pdf);const text=parseText(value.normalized_text,expectedPaths.text);if(manifest===null||pdf===null||text===null)return null;const sourcePages=value.source.pages;const progress=value.progress===null?null:parseProgress(value.progress,sourcePages,profile,workRoot);if(value.progress!==null&&progress===null)return null;const absent=!manifest.exists&&!pdf.exists&&!text.exists;const state=value.state;if(state==="missing"&&(sourcePages<1||progress!==null||value.failure!==null||!absent)||state==="in_progress"&&(progress===null||value.failure!==null||!absent)||state==="committed"&&(sourcePages<1||progress!==null||value.failure!==null||!manifest.exists||manifest.regular!==true||manifest.size<1||!pdf.exists||pdf.regular!==true||pdf.pages!==sourcePages||!text.exists||text.regular!==true||text.utf8!==true||text.size<1)||["invalid","unknown"].includes(state)&&value.failure===null)return null;return value};var bookChapterOutputPath=(slug,chapter)=>`vault/books/${slug}/ch${chapter.slot}-${chapter.slug}.md`;var bookManifestPath=slug=>`processing/chapters/${slug}/manifest.json`;var bookOverviewPath=slug=>`vault/books/${slug}/00-overview.md`;var BOOK_CHAPTER_SLOT=/^\d{2,3}[a-z]{0,2}$/;var BOOK_CHAPTER_TITLE=/^[^\u0000-\u001f\u007f-\u009f]+$/;var validChapterFilename=(value,slot)=>typeof value==="string"&&value.length>=1&&value.length<=128&&value.startsWith(`${slot}_`)&&value.endsWith(".txt")&&!value.includes("/")&&!value.includes("\\")&&!value.includes("..");var validChapterPages=(start,end)=>start===null&&end===null||Number.isInteger(start)&&Number.isInteger(end)&&start>=1&&end>=start;var parseBookIdentity=value=>{if(!isRecord(value)||!exactKeys2(value,["slug","title","authors","year","isbn","publisher","category","confidence"])||!validMaterialSlug(value.slug)||!validString(value.title,1,500)||!validAuthors(value.authors)||!Number.isInteger(value.year)||value.year<1500||value.year>2030||!validNullableString(value.isbn,100)||!validString(value.publisher,2,500)||!["monograph","edited-volume","handbook","other"].includes(value.category)||!["high","medium"].includes(value.confidence))return null;return value};var optionalString=(value,key,minLength,maxLength)=>!Object.hasOwn(value,key)||validString(value[key],minLength,maxLength);var parseBookIntake=value=>{if(!isRecord(value)||!exactKeys2(value,[],["title","isbn","authors","year","publisher","category"])||!Object.hasOwn(value,"title")&&!Object.hasOwn(value,"isbn")||!optionalString(value,"title",1,500)||!optionalString(value,"isbn",1,100)||Object.hasOwn(value,"authors")&&!validAuthors(value.authors)||Object.hasOwn(value,"year")&&(!Number.isInteger(value.year)||value.year<1500||value.year>2030)||!optionalString(value,"publisher",2,500)||Object.hasOwn(value,"category")&&!["monograph","edited-volume","handbook","other"].includes(value.category))return null;return value};var parseBookSeed=value=>{if(!isRecord(value))return null;if(value.state==="provisional"&&exactKeys2(value,["state","requested_slug","hints"])&&validMaterialSlug(value.requested_slug)){const hints=parseBookIntake(value.hints);return hints===null?null:{state:"provisional",requested_slug:value.requested_slug,hints}}if(value.state==="canonical"&&exactKeys2(value,["state","material_slug","identity"])&&validMaterialSlug(value.material_slug)){const identity=parseBookIdentity(value.identity);return identity===null?null:{state:"canonical",material_slug:value.material_slug,identity}}return null};var parseBookStatusObservation=value=>{const observation=parseStatusEnvelope(value,"book");if(observation===null)return null;const facts=observation.facts;const manifest=facts.manifest;const legacyOcr=facts.legacy_ocr;const slug=observation.slug;const expectedSources=[{format:"epub",path:`sources/${slug}.epub`},{format:"pdf",path:`sources/${slug}.pdf`}];const chapters=Array.isArray(facts.chapters)?facts.chapters:null;if(!exactKeys2(facts,["kind","sources","manifest","ocr_generation","legacy_ocr","chapters","overview"])||facts.kind!=="book"||!Array.isArray(facts.sources)||facts.sources.length!==expectedSources.length||!facts.sources.every((item,index)=>isRecord(item)&&exactKeys2(item,["format","artifact"])&&item.format===expectedSources[index].format&&isArtifactObservation(item.artifact)&&item.artifact.path===expectedSources[index].path)||!isRecord(manifest)||!exactKeys2(manifest,["path","present","usable","valid"])||!isArtifactObservation({path:manifest.path,present:manifest.present,usable:manifest.usable})||typeof manifest.valid!=="boolean"||manifest.path!==bookManifestPath(slug)||!isRecord(legacyOcr)||!exactKeys2(legacyOcr,["path","present","usable","source_sha256","total_pages","completed_pages","next_page"])||!isArtifactObservation({path:legacyOcr.path,present:legacyOcr.present,usable:legacyOcr.usable})||legacyOcr.path!==`processing/chapters/${slug}/ocr.progress.json`||(legacyOcr.usable?typeof legacyOcr.source_sha256!=="string"||!/^[0-9a-f]{64}$/.test(legacyOcr.source_sha256)||!Number.isInteger(legacyOcr.total_pages)||legacyOcr.total_pages<1||!Number.isInteger(legacyOcr.completed_pages)||legacyOcr.completed_pages<0||legacyOcr.completed_pages>legacyOcr.total_pages||legacyOcr.next_page!==null&&(!Number.isInteger(legacyOcr.next_page)||legacyOcr.next_page!==legacyOcr.completed_pages+1)||legacyOcr.completed_pages<legacyOcr.total_pages!==(legacyOcr.next_page!==null):legacyOcr.source_sha256!==null||legacyOcr.total_pages!==null||legacyOcr.completed_pages!==null||legacyOcr.next_page!==null)||chapters===null||!chapters.every(chapter=>isRecord(chapter)&&exactKeys2(chapter,["slot","title","filename","slug","word_count","start_page","end_page","input","output"])&&typeof chapter.slot==="string"&&BOOK_CHAPTER_SLOT.test(chapter.slot)&&validString(chapter.title,1,500)&&BOOK_CHAPTER_TITLE.test(chapter.title)&&validChapterFilename(chapter.filename,chapter.slot)&&validMaterialSlug(chapter.slug)&&Number.isInteger(chapter.word_count)&&chapter.word_count>=0&&validChapterPages(chapter.start_page,chapter.end_page)&&isArtifactObservation(chapter.input)&&chapter.input.path===`processing/chapters/${slug}/${chapter.filename}`&&isArtifactObservation(chapter.output)&&chapter.output.path===bookChapterOutputPath(slug,{slot:chapter.slot,slug:chapter.slug}))||new Set(chapters.map(chapter=>chapter.slot)).size!==chapters.length||new Set(chapters.map(chapter=>chapter.filename)).size!==chapters.length||new Set(chapters.map(chapter=>chapter.slug)).size!==chapters.length||(manifest.valid?!manifest.present||!manifest.usable||chapters.length===0:chapters.length!==0)||!isArtifactObservation(facts.overview)||facts.overview.path!==bookOverviewPath(slug))return null;const pdfSource=facts.sources[1];const pdfArtifact=pdfSource.artifact;if(!pdfArtifact.usable){if(facts.ocr_generation!==null)return null}else{const generation=facts.ocr_generation;if(!isRecord(generation)||!isRecord(generation.source)||parseOcrGenerationObservation(generation,{kind:"book",slug,source:generation.source})===null)return null}return observation};var bookObservationAdmitsIdentity=(observation,identity)=>{const diskIdentity=observation.identity;return observation.facts.overview.present&&observation.facts.overview.usable&&isRecord(diskIdentity)&&diskIdentity.title===identity.title&&diskIdentity.year===identity.year&&Array.isArray(diskIdentity.authors)&&diskIdentity.authors.length===identity.authors.length&&diskIdentity.authors.every((author,index)=>author===identity.authors[index])};var parseBookRunInput=raw=>{const invalid=()=>({ok:false,result:invalidMaterialInputResult({kind:"book",slug:requestedLeafSlug(raw)})});const envelope=parseLeafRunEnvelope(raw);if(envelope===null)return invalid();const seed=parseBookSeed(envelope.seed);const observation=parseBookStatusObservation(envelope.observation);if(seed===null||observation===null)return invalid();const materialSlug=seed.state==="provisional"?seed.requested_slug:seed.material_slug;const observations=sparseObservations([{route:{kind:"book",slug:materialSlug},observation}]);const bound=observations?.get(observationKey({kind:"book",slug:materialSlug}))??null;if(observations===null||seed.state==="canonical"&&seed.material_slug!==seed.identity.slug&&(bound===null||!bookObservationAdmitsIdentity(bound,seed.identity)))return invalid();return{ok:true,value:{seed,observations,options:envelope.options,userDecision:envelope.userDecision}}};var parseBookYearDecisionValue=value=>{if(!exactKeys(value,["current_identity","tmp_path","year_evidence","action"]))return null;const decision=value;const currentIdentity=parseBookIdentity(decision.current_identity);const evidence=parseBookYearEvidence(decision.year_evidence);const action=decision.action;if(currentIdentity===null||!validBookTempPath(decision.tmp_path)||evidence===null||evidence.slug_year!==currentIdentity.year||action==="accept-current"&&!["MISMATCH","AMBIGUOUS"].includes(evidence.verdict)||action==="use-recommended-year"&&evidence.verdict!=="MISMATCH"||!["accept-current","use-recommended-year"].includes(action))return null;return{current_identity:currentIdentity,tmp_path:decision.tmp_path,year_evidence:evidence,action}};var validBookMaterialKey=value=>typeof value==="string"&&value.startsWith("book:")&&validMaterialSlug(value.slice("book:".length));var parseBookYearGate=(receipt,currentIdentityValue)=>{const currentIdentity=parseBookIdentity(currentIdentityValue);const terminal=receipt?.terminal;const issue=terminal?.issue;const evidence=parseBookYearEvidence(terminal?.year_evidence);if(currentIdentity===null||receipt?.operation!=="book.acquire"||!validBookMaterialKey(receipt?.material_key)||!exactKeys(terminal,["status","issue","tmp_path","year_evidence","proposed_actions"])||terminal.status!=="needs_input"||!exactKeys(issue,["code","operation","summary","user_question","retryable"])||issue.operation!=="book.acquire"||!validText(issue.summary,1,4e3)||!validText(issue.user_question,1,4e3)||typeof issue.retryable!=="boolean"||!validBookTempPath(terminal.tmp_path)||evidence===null||evidence.slug_year!==currentIdentity.year||(evidence.verdict==="MISMATCH"?issue.code!=="book.year_mismatch"||!sameClosedValue(terminal.proposed_actions,["accept-current","use-recommended-year"]):evidence.verdict==="AMBIGUOUS"?issue.code!=="book.year_ambiguous"||!sameClosedValue(terminal.proposed_actions,["accept-current"]):true))return null;return{kind:"book_year",operation:"book.acquire",material_key:receipt.material_key,current_identity:currentIdentity,question:issue.user_question,tmp_path:terminal.tmp_path,year_evidence:evidence,proposed_actions:terminal.proposed_actions}};var BOOK_STRUCTURE_CONFLICTS=["chapter_boundaries","reading_order","included_material"];var parsedChapter=value=>{if(!exactKeys(value,["title","start","end"])||!validText(value.title,1,500)||!Number.isInteger(value.start)||!Number.isInteger(value.end)||value.start<1||value.end<value.start)return null;return value};var parsedCandidate=value=>{if(!exactKeys(value,["key","label","summary","chapter_count","chapters"])||!validText(value.key,1,80)||!validText(value.label,1,500)||!validText(value.summary,1,2e3)||!Number.isInteger(value.chapter_count)||!Array.isArray(value.chapters)||value.chapters.length<1||value.chapters.length>150||value.chapter_count!==value.chapters.length)return null;const chapters=value.chapters.map(parsedChapter);if(chapters.some(chapter=>chapter===null)||chapters.some((chapter,index)=>index>0&&chapter.start<=chapters[index-1].end))return null;return value};var parsedCandidates=value=>{if(!Array.isArray(value)||value.length<2||value.length>4)return null;const candidates=value.map(parsedCandidate);if(candidates.some(candidate=>candidate===null)||new Set(candidates.map(candidate=>candidate.key)).size!==candidates.length)return null;return candidates};var parsedConflicts=value=>{if(!Array.isArray(value)||value.length<1||value.length>BOOK_STRUCTURE_CONFLICTS.length||value.some(conflict=>typeof conflict!=="string"||!BOOK_STRUCTURE_CONFLICTS.includes(conflict))||new Set(value).size!==value.length)return null;return value};var parseBookStructureGate=receipt=>{const terminal=receipt?.terminal;const issue=terminal?.issue;const candidates=parsedCandidates(terminal?.candidates);const conflicts=parsedConflicts(terminal?.conflicts);if(receipt?.operation!=="book.prepare"||receipt?.format!=="pdf"||!validText(receipt?.material_key,1,512)||terminal?.status!=="needs_input"||issue?.code!=="book.chapter_structure_ambiguous"||issue?.operation!=="book.prepare"||!validText(issue?.user_question,1,4e3)||!validText(terminal?.source_path,1,2048)||terminal.source_path!==receipt.selected_source||candidates===null||conflicts===null)return null;return{kind:"book_structure",operation:"book.prepare",material_key:receipt.material_key,question:issue.user_question,source_path:terminal.source_path,candidates,conflicts}};var parseBookStructureDecisionValue=(value,expectedGate)=>{if(!exactKeys(value,["source_path","candidates","conflicts","selected_candidate"]))return null;const decision=value;if(!validText(decision.source_path,1,2048))return null;const candidates=parsedCandidates(decision.candidates);const conflicts=parsedConflicts(decision.conflicts);const selected=parsedCandidate(decision.selected_candidate);if(candidates===null||conflicts===null||selected===null||!candidates.some(candidate=>sameClosedValue(candidate,selected))||expectedGate!==void 0&&(decision.source_path!==expectedGate.source_path||!sameClosedValue(candidates,expectedGate.candidates)||!sameClosedValue(conflicts,expectedGate.conflicts)))return null;return value};var SHA2562=/^[0-9a-f]{64}$/;var parsePaperIdentity=value=>{if(!isRecord(value)||!exactKeys2(value,["slug","title","authors","year","doi","oa_url","url","journal","confidence"])||!validMaterialSlug(value.slug)||!validString(value.title,1,500)||!validAuthors(value.authors)||!Number.isInteger(value.year)||value.year<1500||value.year>2030||!validNullableString(value.doi,300)||!validNullableString(value.oa_url,2048)||!validNullableString(value.url,2048)||!validString(value.journal,1,500)||!["high","medium"].includes(value.confidence))return null;return value};var optionalString2=(value,key,minLength,maxLength)=>!Object.hasOwn(value,key)||validString(value[key],minLength,maxLength);var parsePaperIntake=value=>{if(!isRecord(value)||!exactKeys2(value,[],["title","doi","authors","year","journal","oa_url","url"])||!Object.hasOwn(value,"title")&&!Object.hasOwn(value,"doi")||!optionalString2(value,"title",1,500)||!optionalString2(value,"doi",1,300)||Object.hasOwn(value,"authors")&&!validAuthors(value.authors)||Object.hasOwn(value,"year")&&(!Number.isInteger(value.year)||value.year<1500||value.year>2030)||!optionalString2(value,"journal",1,500)||!optionalString2(value,"oa_url",1,2048)||!optionalString2(value,"url",1,2048))return null;return value};var parsePaperOwnerConfirmation=value=>{if(!isRecord(value)||!exactKeys2(value,["operation","identity_slug","owner_slug"])||value.operation!=="material.search"||!validMaterialSlug(value.identity_slug)||!validMaterialSlug(value.owner_slug)||value.identity_slug===value.owner_slug)return null;return value};var parsePaperSeed=value=>{if(!isRecord(value))return null;if(value.state==="provisional"&&exactKeys2(value,["state","requested_slug","hints"])&&validMaterialSlug(value.requested_slug)){const hints=parsePaperIntake(value.hints);return hints===null?null:{state:"provisional",requested_slug:value.requested_slug,hints}}if(value.state==="canonical"&&exactKeys2(value,["state","material_slug","identity"],["owner_confirmation"])&&validMaterialSlug(value.material_slug)){const identity=parsePaperIdentity(value.identity);if(identity===null)return null;if(!Object.hasOwn(value,"owner_confirmation"))return{state:"canonical",material_slug:value.material_slug,identity};const ownerConfirmation=parsePaperOwnerConfirmation(value.owner_confirmation);if(ownerConfirmation===null||ownerConfirmation.identity_slug!==identity.slug||ownerConfirmation.owner_slug!==value.material_slug)return null;return{state:"canonical",material_slug:value.material_slug,identity,owner_confirmation:ownerConfirmation}}return null};var parsePaperSourceCandidate=value=>{if(!isRecord(value)||!exactKeys2(value,["format","path","sha256","size"])||!["pdf","txt"].includes(value.format)||typeof value.path!=="string"||typeof value.sha256!=="string"||!SHA2562.test(value.sha256)||!Number.isInteger(value.size)||value.size<1)return null;return value};var parsePaperSourceDecisionValue=value=>{if(!isRecord(value)||!exactKeys2(value,["candidates_fingerprint","source_path"])||typeof value.candidates_fingerprint!=="string"||!SHA2562.test(value.candidates_fingerprint)||typeof value.source_path!=="string"||value.source_path.length===0)return null;return value};var parsePaperStatusObservation=value=>{const observation=parseStatusEnvelope(value,"paper");if(observation===null)return null;const facts=observation.facts;const slug=observation.slug;const expectedPrepared=[`processing/papers/${slug}/source.txt`,`processing/papers/${slug}/ocr.txt`];const expectedSources=[{format:"pdf",path:`sources/${slug}.pdf`},{format:"txt",path:`sources/${slug}.txt`}];if(!exactKeys2(facts,["kind","sources","source_candidates_fingerprint","prepared","legacy_recovery","ocr_generation","canonical"])||facts.kind!=="paper"||typeof facts.source_candidates_fingerprint!=="string"||!SHA2562.test(facts.source_candidates_fingerprint)||!Array.isArray(facts.sources)||facts.sources.length!==expectedSources.length||!facts.sources.every((item,index)=>{if(!isRecord(item)||!exactKeys2(item,["format","artifact","candidate"])||item.format!==expectedSources[index].format||!isArtifactObservation(item.artifact)||item.artifact.path!==expectedSources[index].path)return false;const candidate=parsePaperSourceCandidate(item.candidate);return item.artifact.usable?candidate!==null&&candidate.format===item.format&&candidate.path===item.artifact.path:item.candidate===null})||!isArtifactList(facts.prepared)||facts.prepared.length!==expectedPrepared.length||!facts.prepared.every((artifact,index)=>artifact.path===expectedPrepared[index])||!isRecord(facts.legacy_recovery)||!exactKeys2(facts.legacy_recovery,["pdf","text"])||!isArtifactObservation(facts.legacy_recovery.pdf)||facts.legacy_recovery.pdf.path!==`processing/papers/${slug}/ocr.pdf`||!isArtifactObservation(facts.legacy_recovery.text)||facts.legacy_recovery.text.path!==`processing/papers/${slug}/ocr.txt`||!isArtifactObservation(facts.canonical)||facts.canonical.path!==`vault/papers/${slug}.md`)return null;const firstSource=facts.sources[0];const pdfCandidate=parsePaperSourceCandidate(firstSource.candidate);if(pdfCandidate===null){if(facts.ocr_generation!==null)return null}else if(pdfCandidate.format!=="pdf"||parseOcrGenerationObservation(facts.ocr_generation,{kind:"paper",slug,source:pdfCandidate})===null)return null;return observation};var paperObservationAdmitsIdentity=(observation,identity)=>{const diskIdentity=observation.identity;return observation.facts.canonical.present&&observation.facts.canonical.usable&&isRecord(diskIdentity)&&diskIdentity.title===identity.title&&diskIdentity.year===identity.year&&Array.isArray(diskIdentity.authors)&&diskIdentity.authors.length===identity.authors.length&&diskIdentity.authors.every((author,index)=>author===identity.authors[index])};var validPaperDiskIdentity=value=>isRecord(value)&&validString(value.title,1,500)&&validAuthors(value.authors)&&Number.isInteger(value.year)&&value.year>=1500&&value.year<=2030;var paperObservationAdmitsOwnerContinuation=(observation,seed)=>seed.material_slug===seed.owner_confirmation.owner_slug&&seed.identity.slug===seed.owner_confirmation.identity_slug&&seed.material_slug!==seed.identity.slug&&observation.slug===seed.material_slug&&observation.facts.canonical.present&&observation.facts.canonical.usable&&validPaperDiskIdentity(observation.identity);var parsePaperRunInput=raw=>{const invalid=()=>({ok:false,result:invalidMaterialInputResult({kind:"paper",slug:requestedLeafSlug(raw)})});const envelope=parseLeafRunEnvelope(raw);if(envelope===null||!exactKeys2(envelope.options,[]))return invalid();const seed=parsePaperSeed(envelope.seed);const observation=parsePaperStatusObservation(envelope.observation);if(seed===null||observation===null)return invalid();const materialSlug=seed.state==="provisional"?seed.requested_slug:seed.material_slug;const observations=sparseObservations([{route:{kind:"paper",slug:materialSlug},observation}]);const bound=observations?.get(observationKey({kind:"paper",slug:materialSlug}))??null;const ownerSeed=seed.state==="canonical"&&"owner_confirmation"in seed?seed:null;if(observations===null||seed.state==="canonical"&&seed.material_slug!==seed.identity.slug&&(bound===null||!paperObservationAdmitsIdentity(bound,seed.identity)&&(ownerSeed===null||!paperObservationAdmitsOwnerContinuation(bound,ownerSeed))))return invalid();return{ok:true,value:{seed,observations,options:envelope.options,userDecision:envelope.userDecision}}};var childRoute=value=>{if(!isRecord(value)||!exactKeys2(value,["kind","slug"])||!["paper","book"].includes(value.kind)||!validMaterialSlug(value.slug))return null;return value};var routeKey=route=>observationKey(route);var sameRoute=(left,right)=>left.kind===right.kind&&left.slug===right.slug;var parseAuthorSeed=value=>{if(!isRecord(value)||!exactKeys2(value,["slug","full_name","topic"])||!validMaterialSlug(value.slug)||!validString(value.full_name,1,500)||value.full_name!==value.full_name.trim()||!validString(value.topic,1,2e3)||value.topic!==value.topic.trim())return null;return value};var parseAuthorOptions=value=>{if(!isRecord(value)||!exactKeys2(value,[],["maxBooks","maxPapers"]))return null;if(Object.hasOwn(value,"maxBooks")&&(!Number.isInteger(value.maxBooks)||value.maxBooks<1||value.maxBooks>5))return null;if(Object.hasOwn(value,"maxPapers")&&(!Number.isInteger(value.maxPapers)||value.maxPapers<1||value.maxPapers>10))return null;return value};var parseAuthorStatusObservation=value=>{const observation=parseStatusEnvelope(value,"author");if(observation===null)return null;const canonical=observation.facts.canonical;if(!exactKeys2(observation.facts,["kind","canonical"])||observation.facts.kind!=="author"||!isArtifactObservation(canonical)||canonical.path!==`vault/authors/${observation.slug}.md`||(canonical.usable?!isRecord(observation.identity)||!exactKeys2(observation.identity,["name"])||!validString(observation.identity.name,1,500):observation.identity!==null))return null;return observation};var invalidResult=raw=>{const requested=(()=>{if(!isRecord(raw))return null;if(isRecord(raw.seed)&&validMaterialSlug(raw.seed.slug))return raw.seed.slug;if(isRecord(raw.resume_seed)&&isRecord(raw.resume_seed.seed)&&validMaterialSlug(raw.resume_seed.seed.slug))return raw.resume_seed.seed.slug;return null})();return{ok:false,result:invalidMaterialInputResult({kind:"author",slug:requested})}};var ownerConflictResult=seed=>({ok:false,result:blockedMaterialResult({material:{requested:{kind:"author",slug:seed.slug},canonical:{kind:"author",slug:seed.slug}}},{code:"author.owner_conflict",operation:null,summary:"The exact Author path belongs to another person.",retryable:false,observation_request:null})});var observationAdmitsSeed=(observation,seed)=>!observation.facts.canonical.usable||isRecord(observation.identity)&&observation.identity.name===seed.full_name;var parseLeafShell=value=>{if(!isRecord(value)||!exactKeys2(value,["route","seed","options"])||!isRecord(value.seed)||!isRecord(value.options))return null;const route=childRoute(value.route);if(route===null)return null;if(value.seed.state!=="canonical"||value.seed.material_slug!==route.slug)return null;return value};var parseResumeSeed=value=>{if(!isRecord(value)||!exactKeys2(value,["kind","seed","options","members","decision_member"])||value.kind!=="author"||!Array.isArray(value.members)||value.members.length===0)return null;const seed=parseAuthorSeed(value.seed);const options=parseAuthorOptions(value.options);if(seed===null||options===null)return null;const members=[];const stableKeys=new Set;for(const member of value.members){if(!isRecord(member)||!exactKeys2(member,["member_route","leaf"]))return null;const memberRoute=childRoute(member.member_route);const leaf=parseLeafShell(member.leaf);if(memberRoute===null||leaf===null||memberRoute.kind==="book"&&leaf.route.kind!=="book")return null;const key=routeKey(memberRoute);if(stableKeys.has(key))return null;stableKeys.add(key);members.push({member_route:memberRoute,leaf})}if(members.filter(member=>member.member_route.kind==="book").length>(options.maxBooks??5)||members.filter(member=>member.member_route.kind==="paper").length>(options.maxPapers??10))return null;const decisionMember=value.decision_member===null?null:childRoute(value.decision_member);if(value.decision_member!==null&&(decisionMember===null||!stableKeys.has(routeKey(decisionMember))))return null;return{kind:"author",seed,options,members,decision_member:decisionMember}};var parseChildObservations=value=>{if(!Array.isArray(value))return null;const result=new Map;for(const entry of value){if(!isRecord(entry)||!exactKeys2(entry,["route","observation"]))return null;const route=childRoute(entry.route);if(route===null)return null;const observation=route.kind==="paper"?parsePaperStatusObservation(entry.observation):parseBookStatusObservation(entry.observation);const key=routeKey(route);if(observation===null||observation.slug!==route.slug||result.has(key))return null;result.set(key,observation)}return result};var validLeafInput=(member,observation,decision)=>{const raw={seed:member.leaf.seed,observation,options:member.leaf.options,...decision===null?{}:{userDecision:decision}};return member.leaf.route.kind==="paper"?parsePaperRunInput(raw).ok:parseBookRunInput(raw).ok};var parseCompose=raw=>{if(!exactKeys2(raw,["observation","resume_seed","child_observations"],["userDecision"]))return invalidResult(raw);const resumeSeed4=parseResumeSeed(raw.resume_seed);const observation=parseAuthorStatusObservation(raw.observation);const childObservations=parseChildObservations(raw.child_observations);const hasDecision=Object.hasOwn(raw,"userDecision");const userDecision=hasDecision?parseUserDecision(raw.userDecision):null;if(resumeSeed4===null||observation===null||childObservations===null||observation.slug!==resumeSeed4.seed.slug||hasDecision!==(resumeSeed4.decision_member!==null)||hasDecision&&userDecision===null)return invalidResult(raw);if(!observationAdmitsSeed(observation,resumeSeed4.seed))return ownerConflictResult(resumeSeed4.seed);const expectedKeys=new Set(resumeSeed4.members.map(member=>routeKey(member.leaf.route)));if(childObservations.size!==expectedKeys.size||[...expectedKeys].some(key=>!childObservations.has(key)))return invalidResult(raw);const decisionMember=resumeSeed4.decision_member;const decisionIndex=decisionMember===null?-1:resumeSeed4.members.findIndex(member=>sameRoute(member.member_route,decisionMember));if(decisionIndex>=0&&userDecision?.material_key!==routeKey(resumeSeed4.members[decisionIndex].leaf.route))return invalidResult(raw);for(const[index,member]of resumeSeed4.members.entries()){const observationForMember=childObservations.get(routeKey(member.leaf.route));if(!validLeafInput(member,observationForMember,index===decisionIndex?userDecision:null))return invalidResult(raw)}return{ok:true,value:{mode:"compose",observation,resumeSeed:resumeSeed4,childObservations,userDecision}}};var parseAuthorRunInput=raw=>{if(!isRecord(raw))return invalidResult(raw);if(Object.hasOwn(raw,"resume_seed"))return parseCompose(raw);if(!exactKeys2(raw,["seed","observation","options"]))return invalidResult(raw);const seed=parseAuthorSeed(raw.seed);const observation=parseAuthorStatusObservation(raw.observation);const options=parseAuthorOptions(raw.options);if(seed===null||observation===null||options===null||observation.slug!==seed.slug)return invalidResult(raw);if(!observationAdmitsSeed(observation,seed))return ownerConflictResult(seed);return{ok:true,value:{mode:"discover",seed,observation,options}}};var childObservationFor=(input,route)=>input.childObservations.get(routeKey(route));var childRunInput=(input,member,decision)=>{const observation=childObservationFor(input,member.leaf.route);const raw={seed:member.leaf.seed,observation,options:member.leaf.options,...decision===null?{}:{userDecision:decision}};const parsed=member.leaf.route.kind==="paper"?parsePaperRunInput(raw):parseBookRunInput(raw);if(!parsed.ok)throw new Error("validated Author child became invalid");return parsed.value};var AUTHOR_ARTIFACT_CONTRACT={"artifact_type":"author","document":{"additional_h2":false,"evidence_rules":["只使用 caller 提供并验证的 canonical Book/Paper corpus","每份成员作品首次出现时使用由其 exact canonical path 推导的 wikilink","保持原材料的证据类型，不虚构时间顺序、引文或作品关系","rating 只能由 caller 提供的证据支持；没有证据时省略"],"h1":"使用 frontmatter.name","metadata_lines":[],"section_order":["思想肖像","代表著作","学术轨迹","关键概念","理论网络","金句要点","项目关联"],"sections":[{"description":"2-3 句概括该学者的核心关切和贡献","h2":"思想肖像","kind":"paragraph","required":true},{"description":"仅列专著;没有专著的作者跳过","h2":"代表著作","kind":"paragraph","required":false},{"description":"学者的研究历程","h2":"学术轨迹","kind":"paragraph","required":true},{"description":"该学者提出/常用的核心概念表","h2":"关键概念","kind":"table","required":true},{"description":"该学者对话过的思想家、理论传统","h2":"理论网络","kind":"bullet-list","required":true},{"description":"可引用的代表性论点 / 原文金句","h2":"金句要点","kind":"blockquote-list","required":true},{"child_kind":"paragraph","description":"H3 per project — 项目名作为 H3 标签;reader 渲染为 tabs","h2":"项目关联","kind":"h3-project-tabs","required":true}]},"frontmatter":{"field_order":["type","name","themes","topics","rating"],"json_schema":{"additionalProperties":false,"description":"Profile of a scholar. One file per author.","properties":{"name":{"description":"作者全名,作为该 entity 的展示名","maxLength":120,"minLength":2,"title":"Name","type":"string"},"rating":{"anyOf":[{"maximum":5,"minimum":1,"type":"integer"},{"type":"null"}],"default":null,"description":"整体学术评分(1..5,reader 渲染为 ★);可选","title":"Rating"},"themes":{"description":"研究方向标签数组;允许空但 lint warn '0 themes 可能漏写'","items":{"type":"string"},"title":"Themes","type":"array"},"topics":{"description":"所属 topic 语料的 slug 数组;供前端按成员反查;允许空","items":{"type":"string"},"title":"Topics","type":"array"},"type":{"const":"author","description":"类型判别符,固定为字面量 'author'","title":"Type","type":"string"}},"required":["type","name"],"title":"AuthorSchema","type":"object"}},"identity":{"fields":["name"],"properties":{"name":{"description":"作者全名,作为该 entity 的展示名","maxLength":120,"minLength":2,"title":"Name","type":"string"}},"required":["name"]},"path_pattern":"vault/authors/{slug}.md","schema_version":"quasi.artifact.author/0.1"};var PAPER_ARTIFACT_CONTRACT={"artifact_type":"paper","document":{"additional_h2":false,"evidence_rules":[],"h1":"忠实概括论文标题；不添加装饰性或项目式后缀","metadata_lines":["英文原标题","作者","期刊来源","仅在 identity 提供 DOI 时写 DOI"],"section_order":["核心论点","理论框架","分节摘要","关键概念","核心引用","金句要点","项目关联"],"sections":[{"description":"说明研究问题、中心论点、证据、推理、贡献，以及正文实际提出的限制","h2":"核心论点","kind":"paragraph","required":true},{"description":"理论传统、对话学者和思想资源","h2":"理论框架","kind":"paragraph","required":true},{"child_kind":"paragraph","description":"按原文小节结构 — H3 是原文 sub-section 标题","h2":"分节摘要","kind":"h3-sections","required":true},{"columns":["概念","英文","提出者","定义"],"description":"论文中讨论的核心概念(同名 H2,paper 用 table)","h2":"关键概念","kind":"table","recommended_items":{"max":5,"min":3},"required":true},{"description":"论文实质使用的著作及其在论证中的作用；不补写 input 无法支持的书目信息","h2":"核心引用","kind":"numbered-list","recommended_items":{"max":15,"min":5},"required":true},{"condition":"只有 input 中存在可定位的准确引文时才生成","description":"可引用段落 / 原文金句","h2":"金句要点","kind":"blockquote-list","required":false},{"child_kind":"numbered-list","condition":"只有 caller identity 明确提供项目语境时才生成","description":"","h2":"项目关联","kind":"h3-project-tabs","required":false}]},"frontmatter":{"field_order":["type","title","authors","year","journal","themes","doi","topics","rating"],"json_schema":{"additionalProperties":false,"description":"A journal article analysis.","properties":{"authors":{"description":"作者数组,永远 ≥1 元素","items":{"maxLength":120,"minLength":2,"type":"string"},"minItems":1,"title":"Authors","type":"array"},"doi":{"anyOf":[{"pattern":"^10\\.\\d+/.+","type":"string"},{"type":"null"}],"default":null,"title":"Doi"},"journal":{"description":"期刊名;paper = 期刊论文,必填","maxLength":200,"minLength":2,"title":"Journal","type":"string"},"rating":{"anyOf":[{"maximum":5,"minimum":1,"type":"integer"},{"type":"null"}],"default":null,"title":"Rating"},"themes":{"description":"主题标签数组;paper 必须有主题(不允许空 — 与 chapter 不同)","items":{"type":"string"},"minItems":1,"title":"Themes","type":"array"},"title":{"maxLength":280,"minLength":2,"title":"Title","type":"string"},"topics":{"description":"所属 topic 语料的 slug 数组;供前端按成员反查;允许空","items":{"type":"string"},"title":"Topics","type":"array"},"type":{"const":"paper","title":"Type","type":"string"},"year":{"anyOf":[{"maximum":2030,"minimum":1500,"type":"integer"},{"type":"null"}],"description":"发表年","title":"Year"}},"required":["type","title","authors","year","journal","themes"],"title":"PaperSchema","type":"object"}},"identity":{"fields":["title","authors","year","journal","doi"],"properties":{"authors":{"description":"作者数组,永远 ≥1 元素","items":{"maxLength":120,"minLength":2,"type":"string"},"minItems":1,"title":"Authors","type":"array"},"doi":{"anyOf":[{"pattern":"^10\\.\\d+/.+","type":"string"},{"type":"null"}],"default":null,"title":"Doi"},"journal":{"description":"期刊名;paper = 期刊论文,必填","maxLength":200,"minLength":2,"title":"Journal","type":"string"},"title":{"maxLength":280,"minLength":2,"title":"Title","type":"string"},"year":{"anyOf":[{"maximum":2030,"minimum":1500,"type":"integer"},{"type":"null"}],"description":"发表年","title":"Year"}},"required":["title","authors","year","journal"]},"path_pattern":"vault/papers/{slug}.md","schema_version":"quasi.artifact.paper/0.1"};var CHAPTER_ARTIFACT_CONTRACT={"artifact_type":"chapter","document":{"additional_h2":false,"evidence_rules":[],"h1":"忠实呈现 caller identity 中的 chapter title；chapter label 非 null 时保留其前缀，不添加装饰性后缀","metadata_lines":["可写原文标题、作者和关键词；每一项必须由 input 或 caller identity 支持"],"section_order":["核心论点","理论框架","分节摘要","关键概念","核心引用","金句要点","项目关联"],"sections":[{"description":"章节的中心论点和论证逻辑","h2":"核心论点","kind":"paragraph","required":true},{"description":"理论传统、对话学者和思想资源","h2":"理论框架","kind":"paragraph","required":true},{"child_kind":"paragraph","description":"按原文小节结构 — H3 是原文 sub-section 标题","h2":"分节摘要","kind":"h3-sections","required":true},{"columns":["概念","英文","提出者","定义"],"description":"章节中讨论的核心概念(同名 H2,chapter 用 table)","h2":"关键概念","kind":"table","recommended_items":{"max":5,"min":3},"required":true},{"description":"本章实质使用的著作及其在论证中的作用；不补写 input 无法支持的书目信息","h2":"核心引用","kind":"numbered-list","recommended_items":{"max":15,"min":5},"required":true},{"condition":"只有 input 中存在可定位的准确引文时才生成","description":"可引用段落 / 原文金句","h2":"金句要点","kind":"blockquote-list","required":false},{"child_kind":"numbered-list","condition":"只有 caller identity 明确提供项目语境时才生成","description":"","h2":"项目关联","kind":"h3-project-tabs","required":false}]},"frontmatter":{"field_order":["type","title","authors","year","book","themes","topics","rating"],"json_schema":{"additionalProperties":false,"description":"A chapter analysis. Must live under vault/books/<slug>/.","properties":{"authors":{"description":"章作者;编纂本里可与父书 authors 不同(章节作者 != 书编者)","items":{"maxLength":120,"minLength":2,"type":"string"},"minItems":1,"title":"Authors","type":"array"},"book":{"description":"父书 slug(从文件路径派生,如 'allison-nightwork-1994');vault-wide lint 校验该 slug 真的存在对应的 book 文件","maxLength":200,"minLength":2,"title":"Book","type":"string"},"rating":{"anyOf":[{"maximum":5,"minimum":1,"type":"integer"},{"type":"null"}],"default":null,"title":"Rating"},"themes":{"description":"章节级主题;允许空(章节经常没有独立主题标签)","items":{"type":"string"},"title":"Themes","type":"array"},"title":{"description":"章节展示标题;caller 提供 chapter label 时以其为前缀,未提供时忠实保留 manifest title","maxLength":280,"minLength":2,"title":"Title","type":"string"},"topics":{"description":"所属 topic 语料的 slug 数组;供前端按成员反查;允许空","items":{"type":"string"},"title":"Topics","type":"array"},"type":{"const":"chapter","title":"Type","type":"string"},"year":{"anyOf":[{"maximum":2030,"minimum":1500,"type":"integer"},{"type":"null"}],"description":"通常等于父书 year","title":"Year"}},"required":["type","title","authors","year","book"],"title":"ChapterSchema","type":"object"}},"identity":{"fields":["title","authors","year","book"],"properties":{"authors":{"description":"章作者;编纂本里可与父书 authors 不同(章节作者 != 书编者)","items":{"maxLength":120,"minLength":2,"type":"string"},"minItems":1,"title":"Authors","type":"array"},"book":{"description":"父书 slug(从文件路径派生,如 'allison-nightwork-1994');vault-wide lint 校验该 slug 真的存在对应的 book 文件","maxLength":200,"minLength":2,"title":"Book","type":"string"},"title":{"description":"章节展示标题;caller 提供 chapter label 时以其为前缀,未提供时忠实保留 manifest title","maxLength":280,"minLength":2,"title":"Title","type":"string"},"year":{"anyOf":[{"maximum":2030,"minimum":1500,"type":"integer"},{"type":"null"}],"description":"通常等于父书 year","title":"Year"}},"required":["title","authors","year","book"]},"path_pattern":"vault/books/{book_slug}/ch{slot}-{chapter_slug}.md","schema_version":"quasi.artifact.chapter/0.1"};var BOOK_ARTIFACT_CONTRACT={"artifact_type":"book","document":{"additional_h2":false,"evidence_rules":[],"h1":"使用 frontmatter.title；不添加模板式或项目式后缀","metadata_lines":[],"section_order":["核心论点","章节逻辑","关键概念","理论贡献","精读章节","项目关联"],"sections":[{"description":"综合全部 supplied chapter analyses，说明全书的中心主题、核心论证和证据关系","h2":"核心论点","kind":"paragraph","required":true},{"description":"各章如何构成整体论证;章节间递进/对话/互补关系","h2":"章节逻辑","kind":"paragraph","required":true},{"columns":["概念","英文","提出者","定义"],"description":"全书的核心概念表(同名 H2,book 用 table 形态)","h2":"关键概念","kind":"table","required":true},{"description":"本书对学术领域的整体贡献","h2":"理论贡献","kind":"paragraph","required":true},{"condition":"只推荐 supplied chapter analyses 中实际存在的章节","description":"按优先级排序的推荐精读章节","h2":"精读章节","kind":"numbered-list","required":true},{"child_kind":"paragraph","condition":"只有 caller identity 明确提供项目语境时才生成","description":"","h2":"项目关联","kind":"h3-project-tabs","required":false}]},"frontmatter":{"field_order":["type","title","authors","year","publisher","isbn","doi","category","themes","topics","rating"],"json_schema":{"additionalProperties":false,"description":"Analytical overview of a book. Aligns with BibTeX @book / @collection.","properties":{"authors":{"description":"作者或编者数组;角色由 category 区分;永远数组(单作者也包成 1 元素)","items":{"maxLength":120,"minLength":2,"type":"string"},"minItems":1,"title":"Authors","type":"array"},"category":{"default":"monograph","description":"书籍类别;BibTeX export 时驱动 @book vs @collection + author vs editor","enum":["monograph","edited-volume","handbook","other"],"title":"Category","type":"string"},"doi":{"anyOf":[{"pattern":"^10\\.\\d+/.+","type":"string"},{"type":"null"}],"default":null,"title":"Doi"},"isbn":{"anyOf":[{"type":"string"},{"type":"null"}],"default":null,"description":"ISBN;接受 int|str,coerce 为 str;格式校验留给 lint","title":"Isbn"},"publisher":{"description":"出版社;Phase 1 lint warn,Phase 2 严格必填","maxLength":200,"minLength":2,"title":"Publisher","type":"string"},"rating":{"anyOf":[{"maximum":5,"minimum":1,"type":"integer"},{"type":"null"}],"default":null,"title":"Rating"},"themes":{"description":"主题标签数组","items":{"type":"string"},"title":"Themes","type":"array"},"title":{"description":"书名(含副标题)","maxLength":280,"minLength":2,"title":"Title","type":"string"},"topics":{"description":"所属 topic 语料的 slug 数组;供前端按成员反查;允许空","items":{"type":"string"},"title":"Topics","type":"array"},"type":{"const":"book","title":"Type","type":"string"},"year":{"anyOf":[{"maximum":2030,"minimum":1500,"type":"integer"},{"type":"null"}],"description":"出版年","title":"Year"}},"required":["type","title","authors","year","publisher"],"title":"BookSchema","type":"object"}},"identity":{"fields":["title","authors","year","publisher","isbn","category"],"properties":{"authors":{"description":"作者或编者数组;角色由 category 区分;永远数组(单作者也包成 1 元素)","items":{"maxLength":120,"minLength":2,"type":"string"},"minItems":1,"title":"Authors","type":"array"},"category":{"default":"monograph","description":"书籍类别;BibTeX export 时驱动 @book vs @collection + author vs editor","enum":["monograph","edited-volume","handbook","other"],"title":"Category","type":"string"},"isbn":{"anyOf":[{"type":"string"},{"type":"null"}],"default":null,"description":"ISBN;接受 int|str,coerce 为 str;格式校验留给 lint","title":"Isbn"},"publisher":{"description":"出版社;Phase 1 lint warn,Phase 2 严格必填","maxLength":200,"minLength":2,"title":"Publisher","type":"string"},"title":{"description":"书名(含副标题)","maxLength":280,"minLength":2,"title":"Title","type":"string"},"year":{"anyOf":[{"maximum":2030,"minimum":1500,"type":"integer"},{"type":"null"}],"description":"出版年","title":"Year"}},"required":["title","authors","year","publisher"]},"path_pattern":"vault/books/{slug}/00-overview.md","schema_version":"quasi.artifact.book/0.1"};var OPERATION_CATALOG={"archive.identify":{"kinds":["archive"],"phase":"Search","effect":"readonly","agent":"quasi:archive-agent","artifacts":{}},"archive.collect":{"kinds":["archive"],"phase":"Acquire","effect":"writer","agent":"quasi:archive-agent","artifacts":{"output":"vault/archives/{slug}/archive.md","manifest":"vault/archives/{slug}/manifest.yaml","originals":"vault/archives/{slug}/originals"}},"archive.audit":{"kinds":["archive"],"phase":"Audit","effect":"writer","agent":"quasi:audit-agent","artifacts":{"target":"vault/archives/{slug}/archive.md"}},"topic.discover-archives":{"kinds":["topic"],"phase":"Search","effect":"readonly","agent":"quasi:webcard-agent","artifacts":{}},"webpage.identify":{"kinds":["webpage"],"phase":"Search","effect":"readonly","agent":"quasi:webpage-agent","artifacts":{}},"webpage.capture":{"kinds":["webpage"],"phase":"Acquire","effect":"writer","agent":"quasi:webpage-agent","artifacts":{"snapshot":"vault/webpages/{slug}/snapshot.webarchive"}},"webpage.prepare":{"kinds":["webpage"],"phase":"Prepare","effect":"writer","agent":"quasi:webpage-agent","artifacts":{"snapshot":"vault/webpages/{slug}/snapshot.webarchive","output":"processing/webpages/{slug}/source.md"}},"webpage.analyse":{"kinds":["webpage"],"phase":"Analyse","effect":"writer","agent":"quasi:analyse-agent","artifacts":{"output":"vault/webpages/{slug}/webpage.md"}},"webpage.audit":{"kinds":["webpage"],"phase":"Audit","effect":"writer","agent":"quasi:audit-agent","artifacts":{"target":"vault/webpages/{slug}/webpage.md"}},"material.search":{"kinds":["paper","book"],"phase":"Search","effect":"readonly","agent":"quasi:metadata-agent","artifacts":{}},"paper.acquire":{"kinds":["paper"],"phase":"Acquire","effect":"writer","agent":"quasi:download-agent","artifacts":{"outputPdf":"sources/{slug}.pdf","outputText":"sources/{slug}.txt"}},"paper.prepare":{"kinds":["paper"],"phase":"Prepare","effect":"writer","agent":"quasi:extract-agent","artifacts":{"input":"{input}","sourcePdf":"sources/{slug}.pdf","sourceText":"sources/{slug}.txt","normalized":"processing/papers/{slug}/source.txt","legacyRecoverySource":"processing/papers/{slug}/ocr.pdf","legacyRecoveryText":"processing/papers/{slug}/ocr.txt"}},"paper.ocr":{"kinds":["paper"],"phase":"Prepare","effect":"writer","agent":"quasi:extract-agent","artifacts":{"source":"sources/{slug}.pdf","lock":"processing/papers/{slug}/.ocr-generation.lock","workDir":"processing/papers/{slug}/.ocr-work/{generationKey}","progress":"processing/papers/{slug}/.ocr-work/{generationKey}/ocr.progress.json","generationDir":"processing/papers/{slug}/ocr-generations/{generationKey}","recoveryPdf":"processing/papers/{slug}/ocr-generations/{generationKey}/ocr.pdf","recoveryText":"processing/papers/{slug}/ocr-generations/{generationKey}/ocr.txt","manifest":"processing/papers/{slug}/ocr-generations/{generationKey}/manifest.json"}},"paper.analyse":{"kinds":["paper"],"phase":"Analyse","effect":"writer","agent":"quasi:analyse-agent","artifacts":{"output":"vault/papers/{slug}.md"}},"paper.audit":{"kinds":["paper"],"phase":"Audit","effect":"writer","agent":"quasi:audit-agent","artifacts":{"target":"vault/papers/{slug}.md"}},"book.acquire":{"kinds":["book"],"phase":"Acquire","effect":"writer","agent":"quasi:download-agent","artifacts":{}},"book.prepare":{"kinds":["book"],"phase":"Prepare","effect":"writer","agent":"quasi:extract-agent","artifacts":{"input":"{input}","source":"sources/{slug}.{format}","normalized":"processing/chapters/{slug}/source.txt","recoverySource":"processing/chapters/{slug}/ocr.pdf","ocrProgress":"processing/chapters/{slug}/ocr.progress.json","recoveryText":"processing/chapters/{slug}/ocr.txt","outputDir":"processing/chapters/{slug}","manifest":"processing/chapters/{slug}/manifest.json"}},"book.ocr":{"kinds":["book"],"phase":"Prepare","effect":"writer","agent":"quasi:extract-agent","artifacts":{"source":"sources/{slug}.pdf","lock":"processing/chapters/{slug}/.ocr-generation.lock","workDir":"processing/chapters/{slug}/.ocr-work/{generationKey}","progress":"processing/chapters/{slug}/.ocr-work/{generationKey}/ocr.progress.json","generationDir":"processing/chapters/{slug}/ocr-generations/{generationKey}","recoveryPdf":"processing/chapters/{slug}/ocr-generations/{generationKey}/ocr.pdf","recoveryText":"processing/chapters/{slug}/ocr-generations/{generationKey}/ocr.txt","manifest":"processing/chapters/{slug}/ocr-generations/{generationKey}/manifest.json"}},"chapter.analyse":{"kinds":["book"],"phase":"Analyse","effect":"writer","agent":"quasi:analyse-agent","artifacts":{}},"book.synthesise":{"kinds":["book"],"phase":"Synthesise","effect":"writer","agent":"quasi:synthesis-agent","artifacts":{"output":"vault/books/{slug}/00-overview.md"}},"book.audit":{"kinds":["book"],"phase":"Audit","effect":"writer","agent":"quasi:audit-agent","artifacts":{"target":"vault/books/{slug}"}},"talk.prepare":{"kinds":["talk"],"phase":"Prepare","effect":"writer","agent":"quasi:transcribe-agent","artifacts":{"processingDir":"processing/talks/{slug}","manifest":"processing/talks/{slug}/manifest.json","prepared":"vault/talks/{slug}/recording.mp4","transcript":"vault/talks/{slug}/transcript.md","subtitle":"vault/talks/{slug}/recording.srt","canonical":"vault/talks/{slug}/talk.md"}},"talk.analyse":{"kinds":["talk"],"phase":"Analyse","effect":"writer","agent":"quasi:analyse-agent","artifacts":{"output":"vault/talks/{slug}/talk.md"}},"talk.audit":{"kinds":["talk"],"phase":"Audit","effect":"writer","agent":"quasi:audit-agent","artifacts":{"target":"vault/talks/{slug}/talk.md"}},"translation.prepare":{"kinds":["translation"],"phase":"Prepare","effect":"writer","agent":"quasi:translate-agent","artifacts":{"source":"sources/{slug}.pdf","derivatives":"processing/translations/{slug}-*.pdf","output":"processing/translations/{slug}-{target}.pdf","manifest":"processing/translations/{slug}-{target}.manifest.json","recoverySource":"processing/translations/{slug}-{target}-reocr.pdf"}},"topic.recall":{"kinds":["topic"],"phase":"Recall","effect":"readonly","agent":"general-purpose","artifacts":{}},"topic.steer":{"kinds":["topic"],"phase":"Search","effect":"writer","agent":"quasi:steer-agent","artifacts":{"outputPath":"vault/topics/{slug}/02-outline.md"}},"topic.webcard":{"kinds":["topic"],"phase":"Search","effect":"writer","agent":"quasi:webcard-agent","artifacts":{"cardPath":"vault/topics/{slug}/cards/{target}.md"}},"topic.synthesise.overview":{"kinds":["topic"],"phase":"Synthesise","effect":"writer","agent":"quasi:synthesis-agent","artifacts":{"outlinePath":"vault/topics/{slug}/02-outline.md","outputPath":"vault/topics/{slug}/00-overview.md"}},"topic.synthesise.resources":{"kinds":["topic"],"phase":"Synthesise","effect":"writer","agent":"quasi:synthesis-agent","artifacts":{"outlinePath":"vault/topics/{slug}/02-outline.md","outputPath":"vault/topics/{slug}/01-resources.md"}},"topic.audit":{"kinds":["topic"],"phase":"Audit","effect":"writer","agent":"quasi:audit-agent","artifacts":{"target":"{target}"}},"author.discover-books":{"kinds":["author"],"phase":"Search","effect":"readonly","agent":"quasi:discovery-agent","artifacts":{}},"author.discover-papers":{"kinds":["author"],"phase":"Search","effect":"readonly","agent":"quasi:discovery-agent","artifacts":{}},"author.resolve-membership":{"kinds":["author"],"phase":"Search","effect":"readonly","agent":"general-purpose","artifacts":{"output":"vault/authors/{slug}.md"}},"author.synthesise":{"kinds":["author"],"phase":"Synthesise","effect":"writer","agent":"quasi:synthesis-agent","artifacts":{"output":"vault/authors/{slug}.md"}},"author.audit":{"kinds":["author"],"phase":"Audit","effect":"writer","agent":"quasi:audit-agent","artifacts":{"target":"vault/authors/{slug}.md"}}};var MATERIAL_SLUG2=/^[a-z0-9][a-z0-9-]{0,79}$/;var InputContractError=class extends Error{constructor(message){super(message);this.name="InputContractError"}};function operationContextBase(kind,slug,rawContext,artifactRoles=[]){if(typeof slug!=="string"||!MATERIAL_SLUG2.test(slug))throw new InputContractError(`invalid material slug: ${String(slug)}`);const context=rawContext&&typeof rawContext==="object"?rawContext:{};const artifactRoleSet=new Set(artifactRoles);const passthrough=Object.fromEntries(Object.entries(context).filter(([name])=>!artifactRoleSet.has(name)));const meta=context.meta||{};return{...passthrough,kind,slug,meta,materialKey:context.materialKey||`${kind}:${slug}`,mode:context.mode||"create",diagnostics:context.diagnostics||[],pass:context.pass||1}}var EXACT_TEMPLATE_VALUE=/^\{([A-Za-z][A-Za-z0-9]*)\}$/;var TEMPLATE_VALUE=/\{([A-Za-z][A-Za-z0-9]*)\}/g;var templateValue=(values,name)=>{const value=values[name];if(typeof value!=="string"&&typeof value!=="number")throw new InputContractError(`missing artifact template value: ${name}`);return String(value)};function expandArtifactTemplates(templates,rawContext,context){const raw=rawContext&&typeof rawContext==="object"?rawContext:{};const values={...raw,...context};const expanded=Object.fromEntries(Object.entries(templates||{}).map(([role,template])=>{const exact=EXACT_TEMPLATE_VALUE.exec(template);return[role,exact?templateValue(values,exact[1]):template.replace(TEMPLATE_VALUE,(_match,name)=>templateValue(values,name))]}));return{...expanded,...context}}var STAGE_RECEIPT_VERSION="quasi.stage.receipt/0.3";var STAGE_STATUSES=["complete","needs_input","blocked","failed"];var stageIssueObjectSchema=(operation,{questionRequired=false}={})=>({type:"object",additionalProperties:false,required:["code","operation","summary","user_question","retryable"],properties:{code:{type:"string",minLength:1,maxLength:200},operation:{const:operation},summary:{type:"string",minLength:1,maxLength:4e3},user_question:{type:questionRequired?"string":["string","null"],...questionRequired?{minLength:1}:{},maxLength:4e3},retryable:{type:"boolean"}}});var SCHEMA_DATA_KEYWORDS=new Set(["const","enum","default","examples"]);var constType=value=>{if(value===null)return"null";if(Array.isArray(value))return"array";if(typeof value==="number")return Number.isInteger(value)?"integer":"number";return typeof value};var annotateConstTypes=node=>{if(Array.isArray(node))return node.map(annotateConstTypes);if(!node||typeof node!=="object")return node;const out={};for(const[key,value]of Object.entries(node))out[key]=SCHEMA_DATA_KEYWORDS.has(key)?value:annotateConstTypes(value);if("const"in out&&!("type"in out))out.type=constType(out.const);return out};var terminalPayload=(payloads,status)=>{const payload=payloads&&payloads[status];return payload&&typeof payload==="object"?payload:{}};var stageTerminalBranch=(operation,status,payloads)=>{const payload=terminalPayload(payloads,status);return{type:"object",additionalProperties:false,required:["status","issue",...payload.required||[]],properties:{status:{const:status},issue:status==="complete"?{type:"null"}:stageIssueObjectSchema(operation,{questionRequired:status==="needs_input"}),...payload.properties||{}}}};var stageTerminalSchema=(operation,payloads={})=>({anyOf:STAGE_STATUSES.filter(status=>status!=="needs_input"||Object.prototype.hasOwnProperty.call(payloads,status)&&payloads[status]!=null).map(status=>stageTerminalBranch(operation,status,payloads))});var partitionStageReceiptSchema=fullSchema=>{const properties=fullSchema.properties||{};const stampedKeys=new Set(Object.entries(properties).filter(([,propertySchema])=>propertySchema&&typeof propertySchema==="object"&&Object.prototype.hasOwnProperty.call(propertySchema,"const")).map(([key])=>key));return{modelSchema:{...fullSchema,required:(fullSchema.required||[]).filter(key=>!stampedKeys.has(key)),properties:Object.fromEntries(Object.entries(properties).filter(([key])=>!stampedKeys.has(key)))},stampedValues:Object.fromEntries(Object.entries(properties).filter(([key])=>stampedKeys.has(key)).map(([key,propertySchema])=>[key,propertySchema.const]))}};function stageReceiptPartition({operation,stage,materialKey,effect,required=[],properties={},definitions={},terminalPayloads={}}){const fullSchema=annotateConstTypes({type:"object",additionalProperties:false,required:["schema_version","operation","stage","material_key","effect","attempt",...required,"terminal"],properties:{schema_version:{const:STAGE_RECEIPT_VERSION},operation:{const:operation},stage:{const:stage},material_key:{const:materialKey},effect:{const:effect},attempt:{type:"integer",const:1},...properties,terminal:stageTerminalSchema(operation,terminalPayloads)},...Object.keys(definitions).length===0?{}:{$schema:"http://json-schema.org/draft-07/schema#",definitions}});return partitionStageReceiptSchema(fullSchema)}var asFragment=fragment=>fragment&&typeof fragment==="object"?fragment:{};function defineOperation(descriptor){const{operation,stage,effect,refs:makeRefs,payloadProperties,terminalPayloads,complete,envelope,promptText}=descriptor;const refs=context=>makeRefs(context);const receiptSchema=context=>{const exactRefs=refs(context);const payload=asFragment(payloadProperties(exactRefs));return stageReceiptPartition({operation,stage,materialKey:context.materialKey,effect,required:payload.required||[],properties:payload.properties||{},definitions:payload.definitions||{},terminalPayloads:terminalPayloads?terminalPayloads(exactRefs):{}})};const prompt=context=>{const request=envelope(context,refs(context));return promptText?promptText(request):JSON.stringify(request,null,2)};return{refs,receiptSchema,complete:(receipt,context)=>complete(receipt,context)===true,prompt}}function operationPreparer(kind,operationRows){const rowsByOperation=Object.fromEntries(operationRows.map(row=>[row.operation,row]));const resolveCatalogOperation=operation=>{const identity=OPERATION_CATALOG[operation];const operationRow=rowsByOperation[operation];if(!identity||!identity.kinds.includes(kind)||!operationRow)return null;const descriptor={...operationRow,stage:identity.phase,effect:identity.effect,agentType:identity.agent,artifacts:identity.artifacts||{}};return{kind,operation,descriptor,row:defineOperation(descriptor)}};const prepareOperation4=invocation=>{const resolved=resolveCatalogOperation(invocation?.operation);const fullInvocation={...invocation,kind};if(!resolved)throw unregisteredOperation(fullInvocation);return prepareResolvedOperation(resolved,fullInvocation)};return{prepareOperation:prepareOperation4}}var unregisteredOperation=invocation=>new InputContractError(`operation is not registered for material kind: ${String(invocation?.kind)}:${String(invocation?.operation)}`);function resolveOperationContext(resolved,slug,rawContext){const templates=resolved.descriptor.artifacts;const base=operationContextBase(resolved.kind,slug,rawContext,Object.keys(templates));const context=typeof resolved.descriptor.context==="function"?resolved.descriptor.context(rawContext&&typeof rawContext==="object"?rawContext:{},base):base;return expandArtifactTemplates(templates,rawContext,context)}var validTargetPath=path=>{if(typeof path!=="string"||path.length===0||path.startsWith("/")||path.includes("\\"))return false;const parts=path.split("/");return parts.every(part=>part.length>0&&part!=="."&&part!=="..")&&parts.join("/")===path};var resolvedWriteTargets=(resolved,refs,context)=>{const{effect,writeTargets}=resolved.descriptor;if(effect==="readonly"){if(writeTargets!==void 0)throw new Error(`readonly operation must omit writeTargets: ${resolved.operation}`);return[]}if(typeof writeTargets!=="function")throw new Error(`writer operation has no writeTargets: ${resolved.operation}`);const targets=writeTargets(refs,context);if(!Array.isArray(targets)||targets.length===0)throw new Error(`writer operation has no resolved write target: ${resolved.operation}`);for(const target of targets){if(!target||!["exact","subtree"].includes(target.scope)||!validTargetPath(target.path))throw new Error(`writer operation has invalid write target: ${resolved.operation}`)}return targets};function prepareResolvedOperation(resolved,invocation){if(typeof invocation.label!=="string"||invocation.label.length===0)throw new InputContractError("operation invocation requires a label");const context=resolveOperationContext(resolved,invocation.slug,invocation.context);const refs=resolved.row.refs(context);const prompt=resolved.row.prompt(context);const{modelSchema,stampedValues}=resolved.row.receiptSchema(context);const writeTargets=resolvedWriteTargets(resolved,refs,context);return{invocation,context,prompt,options:{schema:modelSchema,agentType:resolved.descriptor.agentType,phase:resolved.descriptor.stage,label:invocation.label},stampedValues,complete:receipt=>receipt.terminal.status==="complete"&&resolved.row.complete(receipt,context),writeTargets}}var posixSingleQuote=value=>`'${String(value).split("'").join(`'"'"'`)}'`;var ATTEMPT_SCHEMA={type:"array",maxItems:64,items:{type:"object",additionalProperties:false,required:["source","status","error"],properties:{source:{type:"string",minLength:1,maxLength:200},status:{type:"string",minLength:1,maxLength:100},error:{type:["string","null"],maxLength:4e3}}}};var issueSchema=(operation,codes,{questionRequired=false}={})=>({type:"object",additionalProperties:false,required:["code","operation","summary","user_question","retryable"],properties:{code:Array.isArray(codes)?{type:"string",enum:codes}:{const:codes},operation:{const:operation},summary:{type:"string",minLength:1,maxLength:4e3},user_question:{type:questionRequired?"string":["string","null"],...questionRequired?{minLength:1}:{},maxLength:4e3},retryable:{type:"boolean"}}});var PREPARE_STEP_SCHEMA={type:"object",additionalProperties:false,required:["capability","outcome","summary"],properties:{capability:{type:"string",minLength:1,maxLength:100},outcome:{type:"string",enum:["observed","created","reused","repaired","failed"]},summary:{type:"string",minLength:1,maxLength:2e3}}};var AUDIT_DIAGNOSTIC_SCHEMA={type:"object",additionalProperties:false,required:["path","kind","reason"],properties:{path:{type:"string",minLength:1,maxLength:2048,pattern:"^[^/]"},kind:{type:"string",minLength:1,maxLength:200},reason:{type:"string",minLength:1,maxLength:4e3}}};var actionPayloads=({mode,writeState=false})=>({complete:{required:["action",...writeState?["write_state"]:[]],properties:{action:{type:"string",enum:mode==="create"?["create","reconciled"]:["repair","reconciled"]},...writeState?{write_state:{type:"string",enum:["written","not_written"]}}:{}}},failed:{required:["action",...writeState?["write_state"]:[]],properties:{action:{const:mode},...writeState?{write_state:{const:"not_written"}}:{}}},blocked:{required:["action",...writeState?["write_state"]:[]],properties:{action:{const:mode},...writeState?{write_state:{const:"unknown"}}:{}}}});var auditComplete=receipt=>receipt.remaining_violations===0?receipt.escalated.length===0:receipt.remaining_violations===receipt.escalated.length;var auditEnvelopeExtras=()=>({});var makeAuditRow=({operation,refs,targetRole,targetScope,artifactRoles,exactPaths=false,envelopeExtras=auditEnvelopeExtras})=>({operation,refs,writeTargets:({target})=>[{scope:targetScope,path:target}],payloadProperties:({target,pass})=>({required:["target_path","pass",...artifactRoles===void 0?[]:["artifact_roles"],"remaining_violations","escalated","mutated_paths"],properties:{target_path:{const:target},pass:{const:pass},...artifactRoles===void 0?{}:{artifact_roles:{const:artifactRoles}},remaining_violations:{type:"integer",minimum:0},escalated:{type:"array",items:exactPaths?{...AUDIT_DIAGNOSTIC_SCHEMA,properties:{...AUDIT_DIAGNOSTIC_SCHEMA.properties,path:{const:target,...AUDIT_DIAGNOSTIC_SCHEMA.properties.path}}}:AUDIT_DIAGNOSTIC_SCHEMA},mutated_paths:{type:"array",uniqueItems:true,items:exactPaths?{const:target,type:"string",maxLength:2048}:{type:"string",maxLength:2048,pattern:"^[^/]"}}}}),complete:auditComplete,envelope:(context,resolvedRefs)=>{const extras=envelopeExtras(context,resolvedRefs);return{schema_version:"quasi.stage.request/0.2",operation,stage:"Audit",material_key:resolvedRefs.materialKey??context.materialKey,...extras.beforeEffect||{},effect:"writer",...extras.beforePass||{},pass:resolvedRefs.pass,mode:resolvedRefs.pass===1?"audit":"re-audit",target:{role:targetRole,path:resolvedRefs.target},audit_command:{argv:["quasi-audit","--path",resolvedRefs.target],output:"JSON is the default output of this writer command. Do not add --format or --report; those select a separate readonly report interface."},...extras.afterTarget||{}}}});var SLUG_PATTERN="^[a-z0-9][a-z0-9-]{0,79}$";var candidateSchema=kind=>({type:"object",additionalProperties:false,required:kind==="book"?["kind","slug","title","authors","year","isbn","publisher","category","confidence"]:["kind","slug","title","authors","year","doi","oa_url","url","journal","confidence"],properties:{kind:{const:kind},slug:{type:"string",minLength:1,maxLength:80,pattern:SLUG_PATTERN},title:{type:"string",minLength:1,maxLength:500},authors:{type:"array",minItems:1,maxItems:32,items:{type:"string",minLength:1,maxLength:200}},year:{type:"integer",minimum:1500,maximum:2030},...kind==="book"?{isbn:{type:["string","null"],maxLength:100},publisher:{type:"string",minLength:2,maxLength:500},category:{type:"string",enum:["monograph","edited-volume","handbook","other"]}}:{doi:{type:["string","null"],maxLength:300},oa_url:{type:["string","null"],maxLength:2048},url:{type:["string","null"],maxLength:2048},journal:{type:"string",minLength:1,maxLength:500}},confidence:{type:"string",enum:["high","medium"]}}});var resolvedMemberSchema={type:"object",additionalProperties:false,required:["kind","requested_slug","vault_slug","path","match"],properties:{kind:{type:"string",enum:["book","paper"]},requested_slug:{type:"string",minLength:1,maxLength:80,pattern:SLUG_PATTERN},vault_slug:{type:["string","null"],minLength:1,maxLength:80,pattern:SLUG_PATTERN},path:{type:["string","null"],maxLength:2048},match:{type:["string","null"],enum:["slug","isbn","doi","title",null]}}};var discoveryRow=kind=>{const plural=kind==="book"?"books":"papers";const operation=kind==="book"?"author.discover-books":"author.discover-papers";return{operation,context:(rawContext,base)=>({...base,fullName:rawContext.fullName,topic:rawContext.topic,count:rawContext.count}),refs:({materialKey,fullName,topic,count})=>({materialKey,collectionKey:materialKey,kind,fullName,topic,count}),payloadProperties:refs=>({required:["collection_key","kind","full_name","topic","count","candidates"],properties:{collection_key:{const:refs.collectionKey},kind:{const:refs.kind},full_name:{const:refs.fullName},topic:{const:refs.topic},count:{const:refs.count},candidates:{type:"array",maxItems:refs.count,items:candidateSchema(refs.kind)}}}),complete:(receipt,context)=>receipt.candidates.every(candidate=>candidate.authors.includes(context.fullName)),envelope:(_context,refs)=>({schema_version:"quasi.stage.request/0.2",operation,stage:"Search",material_key:refs.materialKey,effect:"readonly",objective:`Find up to ${refs.count} representative ${plural} authored by the exact person and rank them by citations within the requested topic.`,collection_key:refs.collectionKey,kind:refs.kind,full_name:refs.fullName,topic:refs.topic,count:refs.count,sort:"citations",identity_contract:kind==="book"?BOOK_ARTIFACT_CONTRACT.identity:PAPER_ARTIFACT_CONTRACT.identity})}};var membershipRefs=({materialKey,name,output,candidates})=>{const requests=candidates.map(({kind,slug})=>({kind,slug}));const helperItems=[{kind:"author",slug:name},...candidates.map(candidate=>({kind:candidate.kind,slug:candidate.slug,...candidate.kind==="book"?{isbn:candidate.isbn,title:candidate.title,authors:candidate.authors}:{doi:candidate.doi,title:candidate.title,authors:candidate.authors}}))];return{materialKey,collectionKey:materialKey,name,output,requests,helperItems}};var completeMembership=(receipt,context)=>{const requests=context.candidates.map(({kind,slug})=>({kind,slug}));return receipt.resolved.length===requests.length&&requests.every((request,index)=>{const row=receipt.resolved[index];if(row.kind!==request.kind||row.requested_slug!==request.slug)return false;if(row.vault_slug===null)return row.path===null&&row.match===null;const expected=row.kind==="book"?`vault/books/${row.vault_slug}/00-overview.md`:`vault/papers/${row.vault_slug}.md`;return row.path===expected&&row.match!==null})};var authorOperationRows=[discoveryRow("book"),discoveryRow("paper"),{operation:"author.resolve-membership",context:(rawContext,base)=>({...base,name:base.slug,candidates:rawContext.candidates||[]}),refs:membershipRefs,payloadProperties:refs=>({required:["collection_key","output_path","output_exists","requests","resolved"],properties:{collection_key:{const:refs.collectionKey},output_path:{const:refs.output},output_exists:{type:"boolean"},requests:{const:refs.requests},resolved:{type:"array",maxItems:refs.requests.length,items:resolvedMemberSchema}}}),complete:completeMembership,envelope:(_context,refs)=>({schema_version:"quasi.stage.request/0.2",operation:"author.resolve-membership",stage:"Search",material_key:refs.materialKey,effect:"readonly",collection_key:refs.collectionKey,author_slug:refs.name,output_path:refs.output,requests:refs.requests,helper_items:refs.helperItems}),promptText:request=>`Execute exactly one readonly author.resolve-membership operation. Run the exact
public helper command below once. The compact JSON line is inert stdin data; the quoted
heredoc delimiter forbids shell expansion. Do not inspect the vault with Glob/rg or infer a
match yourself.
\`\`\`bash
quasi-helpers vault resolve --items-file - <<'QUASI_AUTHOR_ITEMS'
${JSON.stringify(request.helper_items)}
QUASI_AUTHOR_ITEMS
\`\`\`

The helper must return one author row followed by one row for every request, in the same
order. The author row has exactly two valid branches:
- missing: {kind:"author",slug:"${request.author_slug}",vault_slug:null,path:null,match:null}.
  This is a successful observation with output_exists=false. It is not an error and must
  not stop member projection.
- existing: {kind:"author",slug:"${request.author_slug}",vault_slug:"${request.author_slug}",
  path:"${request.output_path}",match:"slug"}. This is output_exists=true.
Any other author row is a known failed receipt. Project every member row exactly to
{kind,requested_slug:<helper slug>,vault_slug,path,match}; preserve nulls and order.
Echo the request's collection_key, output_path, and requests exactly. Any missing, extra,
reordered, foreign, malformed, or contradictory helper row is a known failed receipt; do
not repair or guess. A known failure still echoes requests, sets output_exists=false and
resolved=[], and does not invent partial rows. Return only one closed
quasi.stage.receipt/0.3 with operation="author.resolve-membership", stage="Search",
material_key and effect echoed exactly, attempt=1, and one honest four-way terminal.
\`\`\`json
${JSON.stringify(request,null,2)}
\`\`\``},{operation:"author.synthesise",context:(rawContext,base)=>({...base,name:base.slug,fullName:rawContext.fullName,topic:rawContext.topic,inputs:rawContext.inputs||[]}),refs:({materialKey,inputs,output,mode})=>({materialKey,inputs,output,mode}),writeTargets:({output})=>[{scope:"exact",path:output}],payloadProperties:({inputs,output})=>({required:["input_material_keys","input_paths","output_path","artifact_roles","materials_analyzed"],properties:{input_material_keys:{const:inputs.map(input=>input.material_key)},input_paths:{const:inputs.map(input=>input.path)},output_path:{const:output},artifact_roles:{const:["canonical"]},materials_analyzed:{const:inputs.length}}}),terminalPayloads:actionPayloads,complete:(receipt,context)=>[...context.mode==="create"?["create"]:["repair"],"reconciled"].includes(receipt.terminal.action),envelope:({materialKey,name,fullName,topic,diagnostics},{inputs,output,mode})=>({schema_version:"quasi.stage.request/0.2",operation:"author.synthesise",stage:"Synthesise",material_key:materialKey,collection_key:materialKey,inputs:inputs.map(input=>({material_key:input.material_key,kind:input.kind,id:input.id,role:"canonical",path:input.path,title:input.title})),input_material_keys:inputs.map(input=>input.material_key),input_paths:inputs.map(input=>input.path),output:{role:"canonical",path:output},identity:{slug:name,full_name:fullName,topic},artifact_contract:AUTHOR_ARTIFACT_CONTRACT,frontmatter_seed:{type:"author",name:fullName},mode,overwrite:mode==="repair",repair_diagnostics:mode==="repair"?diagnostics:[]})},makeAuditRow({operation:"author.audit",refs:({materialKey,target,pass})=>({materialKey,target,pass}),artifactRoles:["canonical"],targetRole:"canonical",targetScope:"exact",exactPaths:true,envelopeExtras:({materialKey},{target})=>({beforeEffect:{collection_key:materialKey},afterTarget:{exact_output:target,composite_debt:true}})})];var authorOperations=operationPreparer("author",authorOperationRows);var prepareOperation=authorOperations.prepareOperation;var IDENTITY_CONFLICTS=["title","authors","year","identifier","edition","publication_type"];var parsedCandidate2=(value,requestedKind)=>{if(!exactKeys(value,["kind","identity"]))return null;const candidate=value;if(candidate.kind==="paper"&&requestedKind!=="book"){const identity=parsePaperIdentity(candidate.identity);return identity===null?null:{kind:"paper",identity}}if(candidate.kind==="book"){const identity=parseBookIdentity(candidate.identity);return identity===null?null:{kind:"book",identity}}return null};var parsedCandidates2=(value,requestedKind)=>{if(!Array.isArray(value)||value.length<1||value.length>4)return null;const candidates=value.map(item=>parsedCandidate2(item,requestedKind));if(candidates.some(candidate=>candidate===null)||candidates.some((candidate,index)=>candidates.slice(0,index).some(prior=>sameClosedValue(candidate,prior))))return null;return candidates};var parsedConflicts2=value=>{if(!Array.isArray(value)||value.length<1||value.length>IDENTITY_CONFLICTS.length||value.some(conflict=>typeof conflict!=="string"||!IDENTITY_CONFLICTS.includes(conflict))||new Set(value).size!==value.length)return null;return value};var parseIdentityConflictGate=(receipt,requestedKind)=>{const terminal=receipt?.terminal;const issue=terminal?.issue;const candidates=parsedCandidates2(terminal?.candidates,requestedKind);const conflicts=parsedConflicts2(terminal?.conflicts);if(receipt?.operation!=="material.search"||receipt?.kind!==requestedKind||!validText(receipt?.material_key,1,512)||terminal?.status!=="needs_input"||issue?.code!=="material.identity_conflict"||issue?.operation!=="material.search"||!validText(issue?.user_question,1,4e3)||candidates===null||conflicts===null)return null;return{kind:"identity_conflict",operation:"material.search",material_key:receipt.material_key,question:issue.user_question,candidates,conflicts}};var parseIdentityConflictDecisionValue=(value,expectedGate,requestedKind)=>{if(!exactKeys(value,["candidates","conflicts","selected_candidate"]))return null;const decision=value;const candidates=parsedCandidates2(decision.candidates,requestedKind);const conflicts=parsedConflicts2(decision.conflicts);const selected=parsedCandidate2(decision.selected_candidate,requestedKind);if(candidates===null||conflicts===null||selected===null||!candidates.some(candidate=>sameClosedValue(candidate,selected))||expectedGate!==void 0&&(!sameClosedValue(candidates,expectedGate.candidates)||!sameClosedValue(conflicts,expectedGate.conflicts)))return null;return value};var SHA256_PATTERN="^[0-9a-f]{64}$";var progressSchema={anyOf:[{type:"null"},{type:"object",additionalProperties:false,required:["completed_pages","total_pages","next_page","ranges"],properties:{completed_pages:{type:"integer",minimum:0},total_pages:{type:"integer",minimum:1},next_page:{type:["integer","null"],minimum:1},ranges:{type:"array",maxItems:1e4,items:{type:"object",additionalProperties:false,required:["start_page","end_page","engine","path","sha256","pages"],properties:{start_page:{type:"integer",minimum:1},end_page:{type:"integer",minimum:1},engine:{type:"string",enum:["dsocr2","tesseract"]},path:{type:"string",minLength:1,maxLength:2048},sha256:{type:"string",pattern:SHA256_PATTERN},pages:{type:"integer",minimum:1}}}}}}]};var fileFactSchema=(path,kind)=>{const extra=kind==="pdf"?{pages:{type:"integer",minimum:0}}:kind==="text"?{utf8:{type:["boolean","null"]},chars:{type:"integer",minimum:0},non_whitespace_chars:{type:"integer",minimum:0}}:{};return{type:"object",additionalProperties:false,required:["path","exists","regular","sha256","size",...Object.keys(extra)],properties:{path:{const:path},exists:{type:"boolean"},regular:{type:["boolean","null"]},sha256:{type:["string","null"],pattern:SHA256_PATTERN},size:{type:"integer",minimum:0},...extra}}};var artifactsSchema=refs=>({type:"array",minItems:3,maxItems:3,uniqueItems:true,items:{anyOf:[fileFactSchema(refs.recoveryPdf,"pdf"),fileFactSchema(refs.recoveryText,"text"),fileFactSchema(refs.manifest,"json")]}});var failureSchema={anyOf:[{type:"null"},{type:"object",additionalProperties:false,required:["code","outcome","retryable","message"],properties:{code:{type:"string",minLength:1,maxLength:200},outcome:{type:"string",enum:["known","unknown"]},retryable:{type:"boolean"},message:{type:"string",minLength:1,maxLength:4e3}}}]};var makeOcrGenerationRow=config=>({operation:config.operation,context:(rawContext,base)=>{const generation=rawContext.ocrGeneration;if(!generation||typeof generation!=="object"||typeof generation.generation_key!=="string"||!/^[0-9a-f]{64}$/.test(generation.generation_key))throw new InputContractError(`${config.operation} requires one current OCR generation`);return{...base,ocrGeneration:generation,generationKey:generation.generation_key}},refs:({slug,materialKey,ocrGeneration,source,lock,workDir,progress,generationDir,recoveryPdf,recoveryText,manifest})=>{const generation=parseOcrGenerationObservation(ocrGeneration,{kind:config.kind,slug,source:ocrGeneration?.source});if(generation===null||generation.material_key!==materialKey||!["missing","in_progress"].includes(generation.state)||generation.profile.validation_policy!==config.validationPolicy||generation.source.path!==source||generation.source.pages<1||generation.paths.lock!==lock||generation.paths.work_dir!==workDir||generation.paths.progress!==progress||generation.paths.generation_dir!==generationDir||generation.paths.pdf!==recoveryPdf||generation.paths.text!==recoveryText||generation.paths.manifest!==manifest||generation.failure!==null)throw new InputContractError(`${config.operation} generation does not match the safe status projection`);return{slug,materialKey,generationKey:generation.generation_key,configFingerprint:generation.config_fingerprint,profile:generation.profile,source,sourceFact:generation.source,pathsFact:generation.paths,expectedState:generation.state,expectedProgress:generation.progress,lock,workDir,progress,generationDir,recoveryPdf,recoveryText,manifest,ocrGeneration:generation}},writeTargets:({lock,workDir,generationDir})=>[{scope:"exact",path:lock},{scope:"subtree",path:workDir},{scope:"subtree",path:generationDir}],payloadProperties:refs=>({required:["generation_key","profile","config_fingerprint","source","paths","state","progress","artifacts","failure"],properties:{generation_key:{const:refs.generationKey},profile:{const:refs.profile},config_fingerprint:{const:refs.configFingerprint},source:{const:refs.sourceFact},paths:{const:refs.pathsFact},state:{type:"string",enum:["in_progress","committed","invalid","unknown"]},progress:progressSchema,artifacts:artifactsSchema(refs),failure:failureSchema}}),terminalPayloads:()=>({complete:{required:["disposition"],properties:{disposition:{type:"string",enum:["partial","created","reconciled"]}}}}),complete:(receipt,context)=>{if(receipt.failure!==null||receipt.artifacts.length!==3)return false;const artifacts=new Map(receipt.artifacts.map(item=>[item.path,item]));if(artifacts.size!==3||!artifacts.has(context.recoveryPdf)||!artifacts.has(context.recoveryText)||!artifacts.has(context.manifest))return false;if(receipt.terminal.disposition==="partial"){const previous=context.ocrGeneration.progress?.completed_pages??0;const progress=receipt.progress;const chunk=context.ocrGeneration.profile.chunk_pages;return receipt.state==="in_progress"&&progress!==null&&progress.total_pages===context.ocrGeneration.source.pages&&progress.completed_pages>previous&&progress.completed_pages<=Math.min(previous+chunk,progress.total_pages)&&progress.next_page===progress.completed_pages+1&&receipt.artifacts.every(item=>!item.exists&&item.regular===null)}const pdf=artifacts.get(context.recoveryPdf);const text=artifacts.get(context.recoveryText);const manifest=artifacts.get(context.manifest);return["created","reconciled"].includes(receipt.terminal.disposition)&&receipt.state==="committed"&&receipt.progress===null&&pdf.exists&&pdf.regular===true&&pdf.pages===context.ocrGeneration.source.pages&&text.exists&&text.regular===true&&text.utf8===true&&text.size>0&&(config.kind==="book"||text.non_whitespace_chars>0)&&manifest.exists&&manifest.regular===true&&manifest.size>0},envelope:({materialKey},refs)=>({schema_version:"quasi.stage.request/0.2",operation:config.operation,stage:"Prepare",material_key:materialKey,effect:"writer",objective:"Advance at most one range of the exact shared OCR generation.",source:refs.sourceFact,generation:{generation_key:refs.generationKey,profile:refs.profile,config_fingerprint:refs.configFingerprint,expected_state:refs.expectedState,expected_progress:refs.expectedProgress},refs:refs.pathsFact,capabilities:[`quasi-extract ocr-generation --kind ${config.kind} --slug ${posixSingleQuote(refs.slug)} --source-file ${posixSingleQuote(refs.source)} --expected-source-sha256 ${posixSingleQuote(refs.sourceFact.sha256)} --generation-key ${posixSingleQuote(refs.generationKey)} --profile ${posixSingleQuote(refs.profile.name)} --json`],execution_contract:["Run the exact ocr-generation capability at most once in this invocation.","Do not background the process, run generic new-work OCR, or write fixed legacy paths.","Preserve the CLI receipt exactly; if the outcome is unclear, stop without replay."]})});var preparedArtifactSchema={type:"array",maxItems:256,items:{type:"object",additionalProperties:false,required:["role","path","exists","usable"],properties:{role:{type:"string",enum:["normalized_document","recovery_source","chapter_manifest","normalized_chapter"]},path:{type:"string",minLength:1,pattern:"^[^/]"},exists:{type:"boolean"},usable:{type:["boolean","null"]}}}};var unusableNormalizedEvidence=(artifacts,normalized)=>artifacts.length===0||artifacts.length===1&&artifacts[0].role==="normalized_document"&&artifacts[0].path===normalized&&artifacts[0].exists===true&&artifacts[0].usable===false;var CHAPTER_SLOT_PATTERN="^\\d{2,3}[a-z]{0,2}$";var CHAPTER_SLUG_PATTERN="^[a-z0-9][a-z0-9-]{0,79}$";var CHAPTER_TITLE_PATTERN="^[^\\u0000-\\u001f\\u007f-\\u009f]+$";var CHAPTER_SLOT=new RegExp(CHAPTER_SLOT_PATTERN);var CHAPTER_SLUG=new RegExp(CHAPTER_SLUG_PATTERN);var bookStructureCandidateSchema={type:"object",additionalProperties:false,required:["key","label","summary","chapter_count","chapters"],properties:{key:{type:"string",minLength:1,maxLength:80},label:{type:"string",minLength:1,maxLength:500},summary:{type:"string",minLength:1,maxLength:2e3},chapter_count:{type:"integer",minimum:1,maximum:150},chapters:{type:"array",minItems:1,maxItems:150,items:{type:"object",additionalProperties:false,required:["title","start","end"],properties:{title:{type:"string",minLength:1,maxLength:500},start:{type:"integer",minimum:1},end:{type:"integer",minimum:1}}}}}};var chapterRefSchema={type:"object",additionalProperties:false,required:["slot","title","filename","slug","word_count","start_page","end_page"],properties:{slot:{type:"string",pattern:CHAPTER_SLOT_PATTERN},title:{type:"string",minLength:1,maxLength:500,pattern:CHAPTER_TITLE_PATTERN},filename:{type:"string"},slug:{type:"string",pattern:CHAPTER_SLUG_PATTERN},word_count:{type:"integer",minimum:0},start_page:{type:["integer","null"],minimum:1},end_page:{type:["integer","null"],minimum:1}}};var validChapterSlot=value=>typeof value==="string"&&CHAPTER_SLOT.test(value);var validChapterRef=chapter=>{const filenameIsSafe=validText(chapter&&chapter.filename,1,128)&&chapter.filename.startsWith(`${chapter.slot}_`)&&chapter.filename.endsWith(".txt")&&!chapter.filename.includes("/")&&!chapter.filename.includes("\\")&&!chapter.filename.includes("..");if(!chapter||!validChapterSlot(chapter.slot)||!CHAPTER_SLUG.test(chapter.slug)||!filenameIsSafe||!validText(chapter.title,1,500))return false;const noPages=chapter.start_page===null&&chapter.end_page===null;const pages=Number.isInteger(chapter.start_page)&&Number.isInteger(chapter.end_page)&&chapter.start_page>=1&&chapter.end_page>=chapter.start_page;return noPages||pages};var uniqueChapters=chapters=>{if(!chapters.length||chapters.some(chapter=>!validChapterRef(chapter)))return false;const slots=new Set;const filenames=new Set;const slugs=new Set;return chapters.every(chapter=>{if(slots.has(chapter.slot)||filenames.has(chapter.filename)||slugs.has(chapter.slug))return false;slots.add(chapter.slot);filenames.add(chapter.filename);slugs.add(chapter.slug);return true})};var chapterActionPayloads=({mode,outputExists})=>{const payloads=actionPayloads({mode,writeState:true});if(mode!=="create")return payloads;payloads.complete.properties.action={const:outputExists?"reconciled":"create"};payloads.complete.properties.write_state={const:outputExists?"not_written":"written"};return payloads};var quoteOrNull=value=>value==null||value===""?null:posixSingleQuote(value);var bookOperationRows=[{operation:"book.acquire",context:(rawContext,base)=>{const formats=rawContext.allowedFormats||["epub","pdf"];const rawYearDecision=rawContext.yearDecision;const yearDecision=rawYearDecision==null?null:parseBookYearDecisionValue(rawYearDecision);if(rawYearDecision!=null&&yearDecision===null)throw new InputContractError("book.acquire requires one coherent year decision");const invocationIdentity=yearDecision===null?null:parseBookIdentity(base.meta);if(yearDecision!==null&&(invocationIdentity===null||(yearDecision.action==="accept-current"?!sameClosedValue(invocationIdentity,yearDecision.current_identity):invocationIdentity.year!==yearDecision.year_evidence.recommended_year)))throw new InputContractError("book.acquire year decision does not bind the invocation identity");return{...base,allowedSources:formats.map(format=>({format,path:`sources/${base.slug}.${format}`})),expectedYear:base.meta.year,invocationIdentity,yearDecision}},refs:({allowedSources,yearDecision})=>({allowedSources,yearDecision}),writeTargets:({allowedSources})=>allowedSources.map(({path})=>({scope:"exact",path})),payloadProperties:({allowedSources})=>({required:["output_path","format","allowed_output_paths","write_state","identity_verified","isbn","attempts"],properties:{output_path:{type:["string","null"],enum:[...allowedSources.map(({path})=>path),null]},format:{type:["string","null"],enum:["epub","pdf",null]},allowed_output_paths:{const:allowedSources.map(({path})=>path)},write_state:{type:"string",enum:["written","not_written","unknown"]},identity_verified:{type:"boolean"},isbn:{type:["string","null"],maxLength:100},attempts:ATTEMPT_SCHEMA}}),terminalPayloads:()=>({complete:{required:["source","tmp_path","year_evidence"],properties:{source:{type:"string",minLength:1,maxLength:200},tmp_path:{type:["string","null"],pattern:BOOK_TEMP_PATH.source},year_evidence:BOOK_YEAR_EVIDENCE_SCHEMA}},failed:{properties:{issue:issueSchema("book.acquire",["book.download_failed","book.candidate_search_unavailable"]),attempts:{...ATTEMPT_SCHEMA,minItems:1}}},blocked:{properties:{issue:issueSchema("book.acquire",["book.acquire_blocked"])}},needs_input:{required:["year_evidence","tmp_path","proposed_actions"],properties:{issue:issueSchema("book.acquire",["book.year_mismatch","book.year_ambiguous"],{questionRequired:true}),year_evidence:BOOK_YEAR_EVIDENCE_SCHEMA,tmp_path:{type:"string",pattern:BOOK_TEMP_PATH.source},proposed_actions:{anyOf:[{const:["accept-current"]},{const:["accept-current","use-recommended-year"]}]}}}}),complete:(receipt,context)=>{const terminal=receipt.terminal;const output=context.allowedSources.find(({path,format})=>receipt.output_path===path&&receipt.format===format);const writeStateCoherent=receipt.write_state==="written"||receipt.write_state==="not_written";const evidence=parseBookYearEvidence(terminal.year_evidence);const decisionEcho=context.yearDecision?terminal.tmp_path===context.yearDecision.tmp_path&&sameClosedValue(evidence,context.yearDecision.year_evidence):evidence!==null&&evidence.slug_year===context.expectedYear&&evidence.verdict==="MATCH";return!!(output&&receipt.identity_verified===true&&writeStateCoherent&&evidence!==null&&decisionEcho)},envelope:({slug,meta,materialKey,invocationIdentity},{allowedSources,yearDecision})=>{const formats=allowedSources.map(({format})=>format);const identity=invocationIdentity||{title:meta.title,authors:meta.authors,year:meta.year,isbn:meta.isbn||null,publisher:meta.publisher,category:meta.category,confidence:meta.confidence==="verified"?"verified":"provided"};return{schema_version:"quasi.stage.request/0.2",operation:"book.acquire",stage:"Acquire",material_key:materialKey,effect:"writer",objective:"Reconcile or obtain one identity-verified Book source at an allowed output path.",allowed_formats:formats,allowed_outputs:allowedSources,refs:{allowed_outputs:allowedSources},identity,current_identity:yearDecision?yearDecision.current_identity:null,identity_contract:BOOK_ARTIFACT_CONTRACT.identity,year_decision:yearDecision,resource_bounds:{accept_budget:1},shell_argv:{slug:posixSingleQuote(slug),allowed_outputs:allowedSources.map(({path})=>posixSingleQuote(path)),expected_title:posixSingleQuote(meta.title),expected_author:posixSingleQuote(meta.authors[0]),year:posixSingleQuote(meta.year),isbn:quoteOrNull(meta.isbn),format_preference:formats.map(format=>posixSingleQuote(format)),year_decision_tmp_path:yearDecision?posixSingleQuote(yearDecision.tmp_path):null},capabilities:["quasi-download book candidates (--title TITLE --author AUTHOR | --query QUERY) [--year YEAR] --format FORMAT... --json","quasi-download book fetch --md5 MD5 --slug SLUG --format FORMAT [--temp-dir DIR] --json","quasi-download accept --path INPUT --slug SLUG --kind book --json","Use available deterministic local tools to inspect or normalize fetched candidates into an allowed PDF/EPUB sibling in the same temporary directory","Read exact candidate, output, and temporary paths to verify identity and year evidence"],output_path_rule:"For complete, echo one request.allowed_outputs[].path byte-for-byte as output_path. A resolved or absolute CLI path is observation evidence only."}}},{operation:"book.prepare",context:(rawContext,base)=>{const rawStructureDecision=rawContext.structureDecision;const structureDecision=rawStructureDecision==null?null:parseBookStructureDecisionValue(rawStructureDecision);if(rawStructureDecision!=null&&structureDecision===null)throw new InputContractError("book.prepare requires one coherent structure decision");return{...base,identity:base.meta,format:rawContext.format,structureDecision,input:rawContext.input??rawContext.source,legacyOcr:rawContext.legacyOcr??null,...rawContext.source?{source:rawContext.source}:{}}},refs:({source,input,format,normalized,recoverySource,ocrProgress,recoveryText,outputDir,manifest,structureDecision,legacyOcr})=>{const generationPdfPattern=new RegExp(`^processing/chapters/[^/]+/ocr-generations/[a-f0-9]{64}/ocr\\.pdf$`);const generationText=generationPdfPattern.test(input)?input.replace(/\/ocr\.pdf$/,"/ocr.txt"):null;if(typeof input!=="string"||input!==source&&input!==recoverySource&&generationText===null)throw new InputContractError("book.prepare input is not one exact current source");if(legacyOcr!==null&&(!legacyOcr||legacyOcr.usable!==true||legacyOcr.path!==ocrProgress||!Number.isInteger(legacyOcr.total_pages)||!Number.isInteger(legacyOcr.completed_pages)||legacyOcr.completed_pages<0||legacyOcr.completed_pages>=legacyOcr.total_pages))throw new InputContractError("book.prepare legacy OCR mode requires one incomplete released progress file");if(structureDecision!==null&&(format!=="pdf"||![source,input,recoverySource].includes(structureDecision.source_path)))throw new InputContractError("book.prepare structure decision does not bind the current PDF source");return{source,input,format,normalized,recoverySource,ocrProgress,recoveryText,outputDir,manifest,structureDecision,generationText,legacyOcr}},writeTargets:({outputDir})=>[{scope:"subtree",path:outputDir}],payloadProperties:refs=>({required:["format","output_dir","selected_source","normalized_path","manifest_path","manifest_fingerprint","mode","disposition","chapter_count","chapters","artifacts","steps","diagnostics"],properties:{format:{const:refs.format},output_dir:{const:refs.outputDir},selected_source:{type:["string","null"],enum:[...new Set([refs.source,refs.input,refs.recoverySource]),null]},normalized_path:{type:["string","null"],enum:[...new Set([refs.normalized,refs.generationText,refs.recoveryText].filter(path=>path!==null)),null]},manifest_path:{const:refs.manifest},manifest_fingerprint:{type:["string","null"],pattern:"^[a-f0-9]{64}$"},mode:{type:["string","null"],enum:["epub","toc","pattern","manual","repair",null]},disposition:{type:["string","null"],enum:["created","reused","replaced","repaired",null]},chapter_count:{type:"integer",minimum:0,maximum:150},chapters:{type:"array",maxItems:150,items:chapterRefSchema},artifacts:preparedArtifactSchema,steps:{type:"array",maxItems:96,items:PREPARE_STEP_SCHEMA},diagnostics:{type:"array",maxItems:96,items:{type:"string",maxLength:4e3}}}}),terminalPayloads:({format,source,input,recoverySource})=>({complete:{required:["disposition"],properties:{disposition:{type:"string",enum:["prepared","ocr_required","legacy_partial","legacy_completed"]}}},...format==="pdf"?{needs_input:{required:["source_path","candidates","conflicts"],properties:{issue:issueSchema("book.prepare","book.chapter_structure_ambiguous",{questionRequired:true}),source_path:{type:"string",enum:[...new Set([source,input,recoverySource])]},candidates:{type:"array",minItems:2,maxItems:4,items:bookStructureCandidateSchema},conflicts:{type:"array",minItems:1,maxItems:3,uniqueItems:true,items:{type:"string",enum:["chapter_boundaries","reading_order","included_material"]}}}}}:{}}),complete:(receipt,context)=>{const terminalDisposition=receipt.terminal.disposition;if(terminalDisposition==="ocr_required")return context.format==="pdf"&&context.input===context.source&&context.legacyOcr===null&&receipt.selected_source===null&&receipt.normalized_path===null&&receipt.manifest_fingerprint===null&&receipt.mode===null&&receipt.disposition===null&&receipt.chapter_count===0&&receipt.chapters.length===0&&unusableNormalizedEvidence(receipt.artifacts,context.normalized);if(terminalDisposition==="legacy_partial"||terminalDisposition==="legacy_completed")return context.legacyOcr!==null&&receipt.selected_source===context.source&&receipt.normalized_path===null&&receipt.manifest_fingerprint===null&&receipt.mode===null&&receipt.disposition===null&&receipt.chapter_count===0&&receipt.chapters.length===0&&receipt.artifacts.length===0;if(terminalDisposition!=="prepared")return false;const chapterPaths=receipt.chapters.map(chapter=>`${context.outputDir}/${chapter.filename}`);const allowed=new Set([context.normalized,context.recoverySource,context.recoveryText,...context.generationText===null?[]:[context.generationText],context.manifest,...chapterPaths]);const listedChapters=new Set(receipt.artifacts.filter(artifact=>artifact.role==="normalized_chapter"&&artifact.exists===true&&artifact.usable===true).map(artifact=>artifact.path));return typeof receipt.selected_source==="string"&&typeof receipt.manifest_fingerprint==="string"&&typeof receipt.mode==="string"&&typeof receipt.disposition==="string"&&receipt.chapter_count===receipt.chapters.length&&uniqueChapters(receipt.chapters)&&receipt.artifacts.every(artifact=>allowed.has(artifact.path))&&chapterPaths.every(path=>listedChapters.has(path))&&receipt.artifacts.some(artifact=>artifact.role==="chapter_manifest"&&artifact.path===receipt.manifest_path&&artifact.exists===true&&artifact.usable===true)},envelope:({materialKey,identity},refs)=>({schema_version:"quasi.stage.request/0.2",operation:"book.prepare",stage:"Prepare",material_key:materialKey,effect:"writer",objective:"Produce and semantically verify one coherent chapter set for the exact accepted Book source, or report that the direct PDF requires shared OCR.",identity,refs:{source:refs.source,input:refs.input,format:refs.format,normalized_document:refs.normalized,generation_text:refs.generationText,recovery_source:refs.recoverySource,legacy_ocr_progress:refs.legacyOcr===null?null:refs.ocrProgress,recovery_text:refs.recoveryText,output_dir:refs.outputDir,manifest:refs.manifest},structure_decision:refs.structureDecision,capabilities:["quasi-extract text INPUT OUTPUT --json",...refs.legacyOcr!==null?[`quasi-extract ocr ${posixSingleQuote(refs.source)} ${posixSingleQuote(refs.recoverySource)} --resume --progress-file ${posixSingleQuote(refs.ocrProgress)} --chunk-pages 8 --no-clobber --json`]:[],"quasi-extract epub INPUT OUTPUT_DIR --json",...refs.format==="pdf"?[`quasi-extract split ${posixSingleQuote(refs.input)} --output-dir ${posixSingleQuote(refs.outputDir)} --method toc|pattern --json`,`quasi-extract split ${posixSingleQuote(refs.input)} --output-dir ${posixSingleQuote(refs.outputDir)} --chapters JSON --json`,`quasi-extract split ${posixSingleQuote(refs.input)} --output-dir ${posixSingleQuote(refs.outputDir)} --pages START-END --title TITLE --slot SLOT --expected-manifest-fingerprint SHA --json`]:[],"Read the exact source, manifest, normalized document, and manifest-listed chapter texts"],disposition_contract:{prepared:"Return only after the committed manifest and representative chapter texts were actually read and hold coherent readable prose.",...refs.format==="pdf"&&refs.legacyOcr===null?{ocr_required:"Allowed only when input is the direct accepted PDF and its extracted normalized_document exists but is semantically unusable (no body text, persistent garbling, or scanned pages without a usable text layer). Stop at that judgement: run no toc, pattern, or manual split; write, reuse, or verify no manifest or chapter files; return selected_source, normalized_path, manifest_fingerprint, mode, and disposition as null, chapter_count 0, chapters [], and artifacts either [] or exactly one normalized_document entry at refs.normalized_document with exists:true and usable:false. Do not start OCR inside book.prepare to reach this disposition; the caller dispatches book.ocr."}:{}},artifact_roles:["normalized_document","recovery_source","chapter_manifest","normalized_chapter"],output_limit:{max_chapters:150}})},makeOcrGenerationRow({operation:"book.ocr",kind:"book",validationPolicy:"book-pdf-v1"}),{operation:"chapter.analyse",context:(rawContext,base)=>{const chapter=rawContext.chapter;if(!chapter||typeof chapter!=="object"||typeof chapter.filename!=="string"||typeof chapter.slot!=="string"||typeof chapter.slug!=="string")throw new InputContractError("chapter.analyse requires one exact chapter context");const outputExists=rawContext.outputExists;if(typeof outputExists!=="boolean")throw new InputContractError("chapter.analyse requires boolean context.outputExists");if(base.mode==="repair"&&!outputExists)throw new InputContractError("chapter.analyse repair requires an existing exact output");return{...base,bookSlug:base.slug,chapter,input:`processing/chapters/${base.slug}/${chapter.filename}`,output:`vault/books/${base.slug}/ch${chapter.slot}-${chapter.slug}.md`,outputExists}},refs:({input,output,outputExists,mode})=>({input,output,outputExists,mode}),writeTargets:({output})=>[{scope:"exact",path:output}],payloadProperties:({input,output})=>({required:["input_path","output_path","artifact_roles"],properties:{input_path:{const:input},output_path:{const:output},artifact_roles:{type:"array",minItems:1,maxItems:1,items:{const:"chapter_canonical"}}}}),terminalPayloads:({mode,outputExists})=>chapterActionPayloads({mode,outputExists}),complete:(receipt,context)=>{const{action,write_state:writeState}=receipt.terminal;if(context.mode==="create")return context.outputExists?action==="reconciled"&&writeState==="not_written":action==="create"&&writeState==="written";return["repair","reconciled"].includes(action)&&writeState===(action==="reconciled"?"not_written":"written")},envelope:({bookSlug,meta,chapter,materialKey,diagnostics},refs)=>{const chapterLabel=chapter.chapter_label||chapter.label||null;const chapterTitle=String(chapter.title||"").trim();const canonicalTitle=chapterLabel===null?chapterTitle:chapterTitle.startsWith(chapterLabel)?chapterTitle:`${chapterLabel} ${chapterTitle}`.trim();const authors=Array.isArray(chapter.authors)&&chapter.authors.length?chapter.authors:meta.authors;return{schema_version:"quasi.stage.request/0.2",operation:"chapter.analyse",stage:"Analyse",material_key:materialKey,input:{role:"normalized_chapter",path:refs.input},output:{role:"chapter_canonical",path:refs.output},output_observation:{path:refs.output,exists:refs.outputExists,authority:"caller"},identity:{book_slug:bookSlug,book_title:meta.title,chapter_slot:chapter.slot,chapter_slug:chapter.slug,chapter_label:chapterLabel,chapter_title:chapter.title,authors,year:meta.year,confidence:meta.confidence==="verified"?"verified":"provided"},artifact_contract:CHAPTER_ARTIFACT_CONTRACT,frontmatter_seed:{type:"chapter",title:canonicalTitle,authors,year:meta.year,book:bookSlug},mode:refs.mode,overwrite:refs.mode==="repair",repair_diagnostics:refs.mode==="repair"?diagnostics:[]}},promptText:request=>`Execute exactly one chapter.analyse operation using this self-contained JSON request.
Do not reinterpret it as another operation and do not read project instruction files.
${JSON.stringify(request,null,2)}`},{operation:"book.synthesise",context:(rawContext,base)=>({...base,inputPaths:rawContext.inputPaths||[]}),refs:({inputPaths,output,mode})=>({inputPaths,output,mode}),writeTargets:({output})=>[{scope:"exact",path:output}],payloadProperties:({inputPaths,output})=>({required:["input_paths","output_path","artifact_roles","chapters_analyzed"],properties:{input_paths:{const:inputPaths},output_path:{const:output},artifact_roles:{type:"array",minItems:1,maxItems:1,items:{const:"canonical"}},chapters_analyzed:{const:inputPaths.length}}}),terminalPayloads:({mode})=>actionPayloads({mode}),complete:(receipt,context)=>[...context.mode==="create"?["create"]:["repair"],"reconciled"].includes(receipt.terminal.action),envelope:({slug,meta,materialKey,diagnostics},refs)=>({schema_version:"quasi.stage.request/0.2",operation:"book.synthesise",stage:"Synthesise",material_key:materialKey,inputs:refs.inputPaths.map(path=>({role:"chapter_canonical",path})),output:{role:"canonical",path:refs.output},identity:{title:meta.title,authors:meta.authors,year:meta.year,publisher:meta.publisher,isbn:meta.isbn||null,category:meta.category,confidence:meta.confidence==="verified"?"verified":"provided"},artifact_contract:BOOK_ARTIFACT_CONTRACT,frontmatter_seed:{type:"book",title:meta.title,authors:meta.authors,year:meta.year,publisher:meta.publisher,isbn:meta.isbn||null,category:meta.category},mode:refs.mode,overwrite:refs.mode==="repair",repair_diagnostics:refs.mode==="repair"?diagnostics:[]})},makeAuditRow({operation:"book.audit",refs:({target,pass})=>({target,pass}),targetRole:"canonical_scope",targetScope:"subtree"})];var MATERIAL_SLUG_PATTERN="^[a-z0-9][a-z0-9-]{0,79}$";var identitySchema=kind=>({type:"object",additionalProperties:false,required:kind==="book"?["slug","title","authors","year","isbn","publisher","category","confidence"]:["slug","title","authors","year","doi","oa_url","url","journal","confidence"],properties:{slug:{type:"string",pattern:MATERIAL_SLUG_PATTERN},title:{type:"string",minLength:1,maxLength:500},authors:{type:"array",minItems:1,maxItems:32,items:{type:"string",minLength:1,maxLength:200}},year:{type:"integer",minimum:1500,maximum:2030},...kind==="book"?{isbn:{type:["string","null"],maxLength:100},publisher:{type:"string",minLength:2,maxLength:500},category:{type:"string",enum:["monograph","edited-volume","handbook","other"]}}:{doi:{type:["string","null"],maxLength:300},oa_url:{type:["string","null"],maxLength:2048},url:{type:["string","null"],maxLength:2048},journal:{type:"string",minLength:1,maxLength:500}},confidence:{type:"string",enum:["high","medium"]}}});var identityDefinition=(kind,requestedKind)=>kind===requestedKind?"identity":"book_identity";var typedIdentitySchema=(kind,requestedKind)=>({type:"object",additionalProperties:false,required:["kind","identity"],properties:{kind:{const:kind},identity:{$ref:`#/definitions/${identityDefinition(kind,requestedKind)}`}}});var candidateSchema2=requestedKind=>requestedKind==="paper"?{anyOf:[typedIdentitySchema("paper",requestedKind),typedIdentitySchema("book",requestedKind)]}:typedIdentitySchema("book",requestedKind);var ownerSlugSchema={type:["string","null"],pattern:MATERIAL_SLUG_PATTERN};var searchIssueSchema={type:"object",additionalProperties:false,required:["code","operation","summary","user_question","retryable"],properties:{code:{type:"string",minLength:1,maxLength:200},operation:{const:"material.search"},summary:{type:"string",minLength:1,maxLength:4e3},user_question:{type:["string","null"],maxLength:4e3},retryable:{type:"boolean"}}};var searchDefinitions=kind=>({identity:identitySchema(kind),...kind==="paper"?{book_identity:identitySchema("book")}:{},issue:searchIssueSchema,conflict_issue:issueSchema("material.search","material.identity_conflict",{questionRequired:true})});var materialSearchOperationRows=[{operation:"material.search",context:(rawContext,base)=>{const rawIdentityDecision=rawContext.identityDecision;const identityDecision=rawIdentityDecision==null?null:parseIdentityConflictDecisionValue(rawIdentityDecision,void 0,base.kind);if(rawIdentityDecision!=null&&(identityDecision===null||identityDecision.selected_candidate.kind!==base.kind))throw new InputContractError("material.search requires one same-kind identity decision");const rawYearDecision=rawContext.yearDecision;const yearDecision=rawYearDecision==null?null:parseBookYearDecisionValue(rawYearDecision);if(rawYearDecision!=null&&(base.kind!=="book"||yearDecision===null||yearDecision.action!=="use-recommended-year"))throw new InputContractError("material.search accepts only a Book recommended-year decision");return{...base,requestedSlug:base.slug,query:rawContext.query,identityDecision,yearDecision}},refs:({materialKey,kind,requestedSlug,query,identityDecision,yearDecision})=>({materialKey,kind,requestedSlug,query,identityDecision,yearDecision:yearDecision||null}),payloadProperties:({kind})=>({required:["kind"],properties:{kind:{const:kind}},definitions:searchDefinitions(kind)}),terminalPayloads:({kind})=>({complete:{required:["identity","owner_slug"],properties:{identity:{$ref:"#/definitions/identity"},owner_slug:ownerSlugSchema}},needs_input:{required:["candidates","conflicts"],properties:{issue:{$ref:"#/definitions/conflict_issue"},candidates:{type:"array",minItems:1,maxItems:4,uniqueItems:true,items:candidateSchema2(kind)},conflicts:{type:"array",minItems:1,maxItems:IDENTITY_CONFLICTS.length,uniqueItems:true,items:{type:"string",enum:IDENTITY_CONFLICTS}}}},blocked:{properties:{issue:{$ref:"#/definitions/issue"}}},failed:{required:kind==="paper"?["webpage_url"]:[],properties:{issue:{$ref:"#/definitions/issue"},...kind==="paper"?{webpage_url:{type:["string","null"],maxLength:2048}}:{}}}}),complete:(receipt,context)=>{const terminal=receipt.terminal;return terminal.status==="complete"&&!!terminal.identity&&(context.identityDecision===null||sameClosedValue(terminal.identity,context.identityDecision.selected_candidate.identity))&&(context.yearDecision===null||terminal.identity.year===context.yearDecision.year_evidence.recommended_year)},envelope:(_context,refs)=>({schema_version:"quasi.stage.request/0.2",operation:"material.search",stage:"Search",material_key:refs.materialKey,effect:"readonly",objective:"Establish the most defensible canonical identity for this exact Book or Paper and reconcile it with any existing local owner.",kind:refs.kind,requested_slug:refs.requestedSlug,query:refs.query,identity_decision:refs.identityDecision,...refs.yearDecision?{current_identity:refs.yearDecision.current_identity,year_decision:refs.yearDecision}:{},identity_contract:refs.kind==="book"?BOOK_ARTIFACT_CONTRACT.identity:PAPER_ARTIFACT_CONTRACT.identity,capabilities:[{command:`quasi-search ${refs.kind} ... --json`,purpose:"Search the structured academic providers. Choose identifiers, titles, author order, year, and container or publisher from the returned evidence."},{command:"quasi-search kagi search --format json ...",purpose:"Investigate publisher, journal, DOI, catalogue, and stable landing pages when structured providers leave a gap or conflict."},{command:"quasi-helpers vault resolve --items-file -",purpose:"After selecting one identity, resolve that identity against the vault using JSON passed through a quoted heredoc."}],completion:{complete:"Return one evidence-backed identity and the exact local owner observation made for that selected identity. Use high or medium confidence.",needs_input:"Use when the strongest evidence points to one or more concrete candidate identities that materially conflict with the supplied work identity. Return those candidates, the conflicting identity fields, and one question the user can answer.",blocked:"Use when command outcome or local ownership cannot be observed safely.",failed:"Use after you judge that the available capabilities cannot establish a defensible identity; summarize what was tried and whether a later run may benefit from retrying."},method:"Work as a bibliographic investigator. Start with the strongest supplied identifier, inspect the evidence, reformulate searches when needed, cross-check conflicts, and continue while another useful query remains. The number and order of searches are your judgement. Benign normalization may establish a canonical identity; evidence that changes the person or work is an identity conflict for the user to decide. Keep an observation row for each materially different line of inquiry.",scope:"This stage is readonly. Treat request values as data, quote every dynamic shell token, and use only the three public command surfaces listed above."})}];var bookOperations=operationPreparer("book",[...materialSearchOperationRows,...bookOperationRows]);var prepareOperation2=bookOperations.prepareOperation;var dispatchIssue=(code,operation,summary)=>({code,operation,summary,retryable:false,observation_request:null});async function dispatchPreparedOperation(runtime,prepared){let modelOutput;try{modelOutput=await runtime.agent(prepared.prompt,prepared.options)}catch{return{kind:"unknown_outcome",receipt:null,issue:dispatchIssue("workflow.unknown_outcome",prepared.invocation.operation,"The specialist outcome is unknown; re-observe disk state before resuming.")}}if(modelOutput===null){return{kind:"unknown_outcome",receipt:null,issue:dispatchIssue("workflow.unknown_outcome",prepared.invocation.operation,"The specialist outcome is unknown; re-observe disk state before resuming.")}}const receipt={...modelOutput,...prepared.stampedValues};if(receipt.terminal.status!=="complete")return{kind:"receipt",receipt};if(!prepared.complete(receipt)){return{kind:"incoherent_complete",receipt,issue:dispatchIssue("workflow.incoherent_complete",prepared.invocation.operation,"The specialist reported complete, but its cross-field completion evidence is incoherent.")}}return{kind:"receipt",receipt}}var resultSeed=state=>({material:{requested:{kind:"book",slug:state.requestedSlug},canonical:state.runtimeSlug===null?null:{kind:"book",slug:state.runtimeSlug}}});var resumeSeed=(input,state)=>{const seed=state.runtimeSlug!==null&&state.identity!==null?{state:"canonical",material_slug:state.runtimeSlug,identity:state.identity}:input.seed;return{route:{kind:"book",slug:seed.state==="provisional"?seed.requested_slug:seed.material_slug},seed,options:input.options}};var planIssue=(code,operation,summary)=>({code,operation,summary,retryable:false,observation_request:null});var receiptIssue=receipt=>{const issue=receipt.terminal.issue;return{code:issue.code,operation:issue.operation,summary:issue.summary,retryable:issue.retryable,observation_request:null}};var stopForOutcome=(state,outcome)=>{if(outcome.kind!=="receipt")return blockedMaterialResult(resultSeed(state),outcome.issue);if(outcome.receipt.terminal.status==="complete")return null;if(outcome.receipt.terminal.status==="needs_input")return blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_gate",outcome.receipt.operation,"The specialist returned a human gate outside its typed Book boundary."));return stoppedMaterialResult(resultSeed(state),outcome.receipt.terminal.status,receiptIssue(outcome.receipt))};var dispatch=(runtime,operation,slug,context)=>dispatchPreparedOperation(runtime,prepareOperation2({operation,slug,context,label:`${slug}:${operation}`}));var allowedFormats=options=>{if(Object.keys(options).some(key=>key!=="allowed_formats"))return null;if(!Object.hasOwn(options,"allowed_formats"))return["epub","pdf"];const formats=options.allowed_formats;if(!Array.isArray(formats)||formats.length<1||formats.length>2||formats.some(format=>!["epub","pdf"].includes(format))||new Set(formats).size!==formats.length)return null;return formats};var manifestReady=observation=>observation!==null&&observation.facts.manifest.present&&observation.facts.manifest.usable&&observation.facts.manifest.valid&&observation.facts.chapters.length>0;var fanoutReady=observation=>manifestReady(observation)&&observation.facts.chapters.every(chapter=>chapter.input.present&&chapter.input.usable);var finalObservedBook=(observation,identity)=>manifestReady(observation)&&observation.facts.chapters.every(chapter=>chapter.output.present&&chapter.output.usable)&&observation.facts.overview.present&&observation.facts.overview.usable&&bookObservationAdmitsIdentity(observation,identity);var sourceFromObservation=(observation,formats)=>{if(observation===null)return null;for(const format of formats){const source=observation.facts.sources.find(candidate=>candidate.format===format);if(source.artifact.present&&source.artifact.usable)return{path:source.artifact.path,format}}return null};var completedBook=(state,chapters)=>{const slug=state.runtimeSlug;return completeMaterialResult(resultSeed(state),[{role:"manifest",path:bookManifestPath(slug)},...chapters.map(chapter=>({role:"chapter",path:bookChapterOutputPath(slug,chapter)})),{role:"overview",path:bookOverviewPath(slug)}],null)};var liftSearchGate=(input,state,receipt)=>{const gate=parseIdentityConflictGate(receipt,"book");return gate===null?blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_gate","material.search","The identity specialist returned an invalid Book conflict gate.")):needsInputMaterialResult(resultSeed(state),receiptIssue(receipt),gate,resumeSeed(input,state))};var bindSearchReceipt=(input,state,receipt)=>{const search=receipt.terminal;state.identity=search.identity;state.runtimeSlug=search.owner_slug===null?state.identity.slug:search.owner_slug;state.observation=input.observations.get(observationKey({kind:"book",slug:state.runtimeSlug}))??null};var auditBook=async(runtime,state,chapters,observedOutputs,currentRunOutputs)=>{const slug=state.runtimeSlug;const common={meta:state.identity,materialKey:`book:${slug}`};const inputPaths=chapters.map(chapter=>bookChapterOutputPath(slug,chapter));const owners=[{path:bookOverviewPath(slug),chapter:null},...chapters.map(chapter=>({path:bookChapterOutputPath(slug,chapter),chapter}))];const ownersByPath=new Map(owners.map(owner=>[owner.path,owner]));const ownerForAuditPath=path=>ownersByPath.get(path);const firstAudit=await dispatch(runtime,"book.audit",slug,{...common,pass:1});const firstStop=stopForOutcome(state,firstAudit);if(firstStop!==null)return firstStop;const firstReceipt=firstAudit.receipt;if(firstReceipt.remaining_violations===0)return completedBook(state,chapters);const escalated=firstReceipt.escalated;if(escalated.some(({path})=>ownerForAuditPath(path)===void 0))return blockedMaterialResult(resultSeed(state),planIssue("workflow.owner_ambiguity","book.audit","Audit escalation targeted an artifact outside this Book."));const repairPath=escalated[0].path;const repairOwner=ownerForAuditPath(repairPath);const diagnostics=escalated.filter(({path})=>path===repairOwner.path);if(repairOwner.chapter!==null){if(!observedOutputs.has(repairOwner.path)&&!currentRunOutputs.has(repairOwner.path))return blockedMaterialResult(resultSeed(state),planIssue("workflow.owner_ambiguity","chapter.analyse","Audit requested repair of a chapter output not established by disk or this run."));const repaired=await dispatch(runtime,"chapter.analyse",slug,{...common,chapter:repairOwner.chapter,mode:"repair",outputExists:true,diagnostics});const repairStop=stopForOutcome(state,repaired);if(repairStop!==null)return repairStop}else{const repaired=await dispatch(runtime,"book.synthesise",slug,{...common,inputPaths,mode:"repair",diagnostics});const repairStop=stopForOutcome(state,repaired);if(repairStop!==null)return repairStop}const secondAudit=await dispatch(runtime,"book.audit",slug,{...common,pass:2});const secondStop=stopForOutcome(state,secondAudit);if(secondStop!==null)return secondStop;const secondReceipt=secondAudit.receipt;if(secondReceipt.escalated.some(({path})=>ownerForAuditPath(path)===void 0))return blockedMaterialResult(resultSeed(state),planIssue("workflow.owner_ambiguity","book.audit","The second audit targeted an artifact outside this Book."));if(secondReceipt.remaining_violations===0)return completedBook(state,chapters);return blockedMaterialResult(resultSeed(state),planIssue("workflow.repair_exhausted","book.audit","The bounded Book repair completed, but the second audit still found violations."))};async function runBookPlanResult(runtime,input,rememberContinuation){const formats=allowedFormats(input.options);const requestedSlug=input.seed.state==="provisional"?input.seed.requested_slug:input.seed.material_slug;const initialObservation=input.observations.get(observationKey({kind:"book",slug:requestedSlug}));const state={requestedSlug,runtimeSlug:input.seed.state==="canonical"?input.seed.material_slug:null,identity:input.seed.state==="canonical"?input.seed.identity:null,observation:initialObservation};rememberContinuation(resumeSeed(input,state));if(formats===null)return blockedMaterialResult(resultSeed(state),planIssue("material.invalid_input",null,"Book options accept only one unique allowed_formats ordering."));const rawYearDecision=input.userDecision?.operation==="book.acquire"?input.userDecision.value:null;const parsedYearDecision=rawYearDecision===null?null:parseBookYearDecisionValue(rawYearDecision);const rawStructureDecision=input.userDecision?.operation==="book.prepare"?input.userDecision.value:null;const parsedStructureDecision=rawStructureDecision===null?null:parseBookStructureDecisionValue(rawStructureDecision);const admittedCanonical=input.seed.state==="canonical"&&bookObservationAdmitsIdentity(initialObservation,input.seed.identity);const searchKey=`book:${requestedSlug}`;const matchedDecision=input.userDecision?.material_key===searchKey&&input.userDecision.operation==="material.search";const pendingAcquireIdentityDecision=matchedDecision&&!fanoutReady(initialObservation)&&sourceFromObservation(initialObservation,formats)===null;if(!admittedCanonical||pendingAcquireIdentityDecision){const rawDecision=decisionForOperation(input.userDecision,searchKey,"material.search",false);const identityDecision=matchedDecision?parseIdentityConflictDecisionValue(rawDecision,void 0,"book"):null;if(matchedDecision&&identityDecision===null)return blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_gate","material.search","The identity decision does not echo one valid Book candidate set and selection."));const query=identityDecision?.selected_candidate.identity??(input.seed.state==="provisional"?input.seed.hints:input.seed.identity);const searchSlug=identityDecision?.selected_candidate.identity.slug??requestedSlug;const searched=await dispatch(runtime,"material.search",searchSlug,{materialKey:searchKey,meta:query,query,...identityDecision===null?{}:{identityDecision}});if(searched.kind==="receipt"&&searched.receipt.terminal.status==="needs_input")return liftSearchGate(input,state,searched.receipt);const searchStop=stopForOutcome(state,searched);if(searchStop!==null)return searchStop;const receipt=searched.receipt;bindSearchReceipt(input,state,receipt);const search=receipt.terminal;if(input.seed.state==="canonical"&&identityDecision===null&&(sourceFromObservation(initialObservation,formats)!==null||manifestReady(initialObservation)))state.identity={...state.identity,isbn:input.seed.identity.isbn};rememberContinuation(resumeSeed(input,state));if(search.owner_slug!==null)return completeMaterialResult(resultSeed(state),[{role:"overview",path:bookOverviewPath(search.owner_slug)}],null);if(state.observation===null)return needsObservationMaterialResult(resultSeed(state),[{kind:"book",slug:state.runtimeSlug}],resumeSeed(input,state))}if(finalObservedBook(state.observation,state.identity)){const observedOutputs2=new Set(state.observation.facts.chapters.filter(chapter=>chapter.output.present).map(chapter=>chapter.output.path));return auditBook(runtime,state,state.observation.facts.chapters,observedOutputs2,new Set)}let source=sourceFromObservation(state.observation,formats);let appliedYearDecision=null;if(!fanoutReady(state.observation)&&source===null){const currentKey=`book:${state.runtimeSlug}`;const matchedYearEnvelope=input.userDecision?.operation==="book.acquire"&&input.userDecision.material_key===currentKey;if(matchedYearEnvelope&&rawYearDecision!==null){if(parsedYearDecision===null)return blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_gate","book.acquire","The Book year decision is structurally incoherent."));if(sameClosedValue(parsedYearDecision.current_identity,state.identity)){appliedYearDecision=parsedYearDecision;if(parsedYearDecision.action==="use-recommended-year"){const searched=await dispatch(runtime,"material.search",state.runtimeSlug,{materialKey:currentKey,meta:state.identity,query:state.identity,yearDecision:parsedYearDecision});if(searched.kind==="receipt"&&searched.receipt.terminal.status==="needs_input")return liftSearchGate(input,state,searched.receipt);const searchStop=stopForOutcome(state,searched);if(searchStop!==null)return searchStop;const receipt2=searched.receipt;bindSearchReceipt(input,state,receipt2);const search=receipt2.terminal;rememberContinuation(resumeSeed(input,state));if(search.owner_slug!==null)return completeMaterialResult(resultSeed(state),[{role:"overview",path:bookOverviewPath(search.owner_slug)}],null)}}}const slug2=state.runtimeSlug;const acquired=await dispatch(runtime,"book.acquire",slug2,{meta:state.identity,materialKey:`book:${slug2}`,allowedFormats:formats,...appliedYearDecision===null?{}:{yearDecision:appliedYearDecision}});if(acquired.kind==="receipt"&&acquired.receipt.terminal.status==="needs_input"){const gate=parseBookYearGate(acquired.receipt,state.identity);return gate===null?blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_gate","book.acquire","The download specialist returned an invalid Book year gate.")):needsInputMaterialResult(resultSeed(state),receiptIssue(acquired.receipt),gate,resumeSeed(input,state))}const acquireStop=stopForOutcome(state,acquired);if(acquireStop!==null)return acquireStop;const receipt=acquired.receipt;state.identity={...state.identity,isbn:receipt.isbn};rememberContinuation(resumeSeed(input,state));source={path:receipt.output_path,format:receipt.format};if(receipt.format==="pdf"||state.observation===null)return needsObservationMaterialResult(resultSeed(state),[{kind:"book",slug:state.runtimeSlug}],resumeSeed(input,state))}let chapters;if(fanoutReady(state.observation)){chapters=state.observation.facts.chapters}else{const slug2=state.runtimeSlug;const currentKey=`book:${slug2}`;const ocrGeneration=source.format==="pdf"?state.observation?.facts.ocr_generation??null:null;const legacyOcr=state.observation?.facts.legacy_ocr??null;if(source.format==="pdf"&&ocrGeneration===null)return blockedMaterialResult(resultSeed(state),planIssue("book.ocr_status_missing","book.ocr","The accepted Book PDF has no current expected OCR generation status."));if(ocrGeneration!==null&&["invalid","unknown"].includes(ocrGeneration.state))return blockedMaterialResult(resultSeed(state),planIssue(ocrGeneration.failure??"book.ocr_generation_unavailable","book.ocr",`The current expected Book OCR generation is ${ocrGeneration.state} and cannot be advanced safely.`));if(legacyOcr?.usable===true&&ocrGeneration?.state==="in_progress")return blockedMaterialResult(resultSeed(state),planIssue("book.ocr_state_ambiguous","book.ocr","Released fixed-path OCR progress and a current shared generation are both active."));const recoverySource=`processing/chapters/${slug2}/ocr.pdf`;let prepareInput=source.path;let incompleteLegacyOcr=null;if(ocrGeneration?.state==="committed"){prepareInput=ocrGeneration.paths.pdf}else if(source.format==="pdf"&&legacyOcr?.usable===true){if(legacyOcr.completed_pages<legacyOcr.total_pages)incompleteLegacyOcr=legacyOcr;else prepareInput=recoverySource}else if(ocrGeneration?.state==="in_progress"){const ocr=await dispatch(runtime,"book.ocr",slug2,{meta:state.identity,materialKey:currentKey,ocrGeneration});const ocrStop=stopForOutcome(state,ocr);if(ocrStop!==null)return ocrStop;return needsObservationMaterialResult(resultSeed(state),[{kind:"book",slug:slug2}],resumeSeed(input,state))}let structureDecision=null;const matchedStructureEnvelope=input.userDecision?.operation==="book.prepare"&&input.userDecision.material_key===currentKey;if(matchedStructureEnvelope&&source.format==="pdf"){if(parsedStructureDecision===null)return blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_gate","book.prepare","The Book structure decision is structurally incoherent."));if([source.path,prepareInput,recoverySource].includes(parsedStructureDecision.source_path))structureDecision=parsedStructureDecision}const prepared=await dispatch(runtime,"book.prepare",slug2,{meta:state.identity,materialKey:currentKey,source:source.path,input:prepareInput,format:source.format,...incompleteLegacyOcr===null?{}:{legacyOcr:incompleteLegacyOcr},...structureDecision===null?{}:{structureDecision}});if(prepared.kind==="receipt"&&prepared.receipt.terminal.status==="needs_input"){const gate=parseBookStructureGate(prepared.receipt);return gate===null?blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_gate","book.prepare","The extraction specialist returned an invalid Book structure gate.")):needsInputMaterialResult(resultSeed(state),receiptIssue(prepared.receipt),gate,resumeSeed(input,state))}const prepareStop=stopForOutcome(state,prepared);if(prepareStop!==null)return prepareStop;const prepareReceipt=prepared.receipt;const prepareDisposition=prepareReceipt.terminal.disposition;if(prepareDisposition==="legacy_partial"||prepareDisposition==="legacy_completed")return needsObservationMaterialResult(resultSeed(state),[{kind:"book",slug:slug2}],resumeSeed(input,state));if(prepareDisposition==="ocr_required"){if(ocrGeneration?.state!=="missing"||incompleteLegacyOcr!==null)return blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_complete","book.prepare","Book Prepare requested shared OCR without one missing current generation."));const ocr=await dispatch(runtime,"book.ocr",slug2,{meta:state.identity,materialKey:currentKey,ocrGeneration});const ocrStop=stopForOutcome(state,ocr);if(ocrStop!==null)return ocrStop;return needsObservationMaterialResult(resultSeed(state),[{kind:"book",slug:slug2}],resumeSeed(input,state))}if(prepareDisposition!=="prepared")return blockedMaterialResult(resultSeed(state),planIssue("workflow.incoherent_complete","book.prepare","Book Prepare returned an unknown complete disposition."));chapters=prepareReceipt.chapters}const slug=state.runtimeSlug;const observedOutputs=new Set((state.observation?.facts.chapters??[]).filter(chapter=>chapter.output.present).map(chapter=>chapter.output.path));const common={meta:state.identity,materialKey:`book:${slug}`};rememberContinuation(resumeSeed(input,state));const preparedChapters=chapters.filter(chapter=>{const observedChapter=state.observation?.facts.chapters.find(({output})=>output.path===bookChapterOutputPath(slug,chapter));return observedChapter?.output.usable!==true}).map(chapter=>prepareOperation2({operation:"chapter.analyse",slug,label:`${slug}:analyse:${chapter.slug}`,context:{...common,chapter,outputExists:observedOutputs.has(bookChapterOutputPath(slug,chapter))}}));const outcomes=await runtime.pipeline(preparedChapters,operation=>dispatchPreparedOperation(runtime,operation));const observationRequired=outcomes.find(outcome=>outcome.kind==="unknown_outcome"||outcome.kind==="incoherent_complete"||outcome.kind==="receipt"&&outcome.receipt.terminal.status==="blocked"&&outcome.receipt.terminal.issue.code===`${outcome.receipt.operation}.output_observation_mismatch`&&outcome.receipt.terminal.issue.retryable===true);if(observationRequired!==void 0)return needsObservationMaterialResult(resultSeed(state),[{kind:"book",slug}],resumeSeed(input,state));for(const outcome of outcomes){const stop=stopForOutcome(state,outcome);if(stop!==null)return stop}const currentRunOutputs=new Set(outcomes.map(outcome=>outcome.receipt.output_path));const inputPaths=chapters.map(chapter=>bookChapterOutputPath(slug,chapter));const synthesised=await dispatch(runtime,"book.synthesise",slug,{...common,inputPaths,mode:state.observation?.facts.overview.present?"repair":"create"});const synthesisStop=stopForOutcome(state,synthesised);if(synthesisStop!==null)return synthesisStop;return auditBook(runtime,state,chapters,observedOutputs,currentRunOutputs)}async function runBookPlanForComposition(runtime,input){let continuation=null;const result=await runBookPlanResult(runtime,input,current=>{continuation=current});return{result,continuation}}var preparedArtifactSchema2=paths=>({type:"array",minItems:1,maxItems:paths.length,uniqueItems:true,items:{type:"object",additionalProperties:false,required:["role","path","exists","usable"],properties:{role:{const:"normalized_text"},path:{type:"string",enum:paths},exists:{type:"boolean"},usable:{type:["boolean","null"]}}}});var generationTextPath=(slug,generationKey)=>`processing/papers/${slug}/ocr-generations/${generationKey}/ocr.txt`;var paperPrepareContext=(rawContext,base)=>{const source=rawContext.source;const input=rawContext.input??source;const sourceCandidate=parsePaperSourceCandidate(rawContext.sourceCandidate);const sourcePdf=`sources/${base.slug}.pdf`;const sourceText=`sources/${base.slug}.txt`;if(![sourcePdf,sourceText].includes(source))throw new InputContractError("paper.prepare source must be one declared Paper source");if(sourceCandidate===null||sourceCandidate.path!==source||sourceCandidate.format!==(source===sourcePdf?"pdf":"txt"))throw new InputContractError("paper.prepare source candidate must bind the exact observed source");if(input===source)return{...base,source,sourceCandidate,input,inputKind:source===sourcePdf?"source_pdf":"source_text",generationKey:null};const generationKey=rawContext.generationKey;if(source!==sourcePdf||typeof generationKey!=="string"||!/^[0-9a-f]{64}$/.test(generationKey)||input!==generationTextPath(base.slug,generationKey))throw new InputContractError("paper.prepare generation input must be the exact committed OCR text");return{...base,source,sourceCandidate,input,inputKind:"generation_text",generationKey}};var quoteOrNull2=value=>value==null||value===""?null:posixSingleQuote(value);var paperOperationRows=[{operation:"paper.acquire",refs:({outputPdf,outputText,meta})=>({outputPdf,outputText,doi:meta.doi||null}),writeTargets:({outputPdf,outputText})=>[{scope:"exact",path:outputPdf},{scope:"exact",path:outputText}],payloadProperties:({outputPdf,outputText,doi})=>({required:["output_path","doi","write_state","identity_verified","attempts"],properties:{output_path:{type:"string",enum:[outputPdf,outputText]},doi:{const:doi},write_state:{type:"string",enum:["written","not_written","unknown"]},identity_verified:{type:"boolean"},attempts:ATTEMPT_SCHEMA}}),terminalPayloads:()=>({complete:{required:["source"],properties:{source:{type:"string",minLength:1,maxLength:200}}},failed:{properties:{attempts:{...ATTEMPT_SCHEMA,minItems:1},issue:issueSchema("paper.acquire","paper.download_failed")}},blocked:{properties:{issue:issueSchema("paper.acquire","paper.acquire_blocked")}}}),complete:receipt=>receipt.identity_verified===true&&(receipt.write_state==="written"||receipt.write_state==="not_written"),envelope:({slug,meta,materialKey},{outputPdf,outputText})=>({schema_version:"quasi.stage.request/0.2",operation:"paper.acquire",stage:"Acquire",material_key:materialKey,effect:"writer",objective:"Reconcile or obtain one identity-verified Paper source at exactly one allowed output path.",allowed_outputs:[{format:"pdf",path:outputPdf},{format:"txt",path:outputText}],refs:{output_pdf:outputPdf,output_text:outputText},identity:{slug,title:meta.title,authors:meta.authors,year:meta.year,journal:meta.journal,doi:meta.doi||null,oa_url:meta.oa_url||null,url:meta.url||null,confidence:meta.confidence==="verified"?"verified":"provided"},identity_contract:PAPER_ARTIFACT_CONTRACT.identity,shell_argv:{slug:posixSingleQuote(slug),exact_output_pdf:posixSingleQuote(outputPdf),exact_output_text:posixSingleQuote(outputText),expected_title:posixSingleQuote(meta.title),expected_author:posixSingleQuote(meta.authors[0]),doi:quoteOrNull2(meta.doi),oa_url:quoteOrNull2(meta.oa_url),url:quoteOrNull2(meta.url)},capabilities:["quasi-download paper fetch --slug SLUG (--doi DOI | --url URL ...) [--title TITLE] [--author AUTHOR] [--temp-dir DIR] [--budget-seconds 30..540] --json","quasi-download paper diagnose --url URL [--via-ezproxy] [--timeout SECONDS] --json","quasi-search kagi ...","quasi-download accept --path INPUT --slug SLUG --kind paper --json","Use available deterministic local tools to inspect or normalize fetched content into one allowed PDF or UTF-8 text output format in the same temporary directory","paper fetch may return identity_uncertain candidates; review each exact temp_path and inspect evidence, accept at most one, and remove the rejected returned temp paths","Read the exact output only to verify title, authors, and DOI evidence"],output_path_rule:"Echo one allowed_outputs[].path byte-for-byte as output_path in every terminal; a resolved or absolute CLI path is observation evidence only."})},{operation:"paper.prepare",context:paperPrepareContext,refs:({source,sourceCandidate,input,inputKind,generationKey,sourcePdf,sourceText,normalized,legacyRecoverySource,legacyRecoveryText})=>{const expectedInput=inputKind==="generation_text"?generationTextPath(sourcePdf.slice("sources/".length,-".pdf".length),generationKey):source;if(input!==expectedInput)throw new InputContractError("paper.prepare input does not match its declared role");return{source,sourceCandidate,input,inputKind,generationKey,sourcePdf,sourceText,normalized,legacyRecoverySource,legacyRecoveryText}},writeTargets:({normalized})=>[{scope:"exact",path:normalized}],payloadProperties:refs=>{const selected=refs.inputKind==="generation_text"?refs.input:refs.normalized;return{required:["source_path","input_path","input_kind","selected_input","artifacts","steps","diagnostics"],properties:{source_path:{const:refs.source},input_path:{const:refs.input},input_kind:{const:refs.inputKind},selected_input:{type:["string","null"],enum:[selected,null]},artifacts:preparedArtifactSchema2(refs.inputKind==="generation_text"?[refs.input]:[refs.normalized]),steps:{type:"array",maxItems:64,items:PREPARE_STEP_SCHEMA},diagnostics:{type:"array",maxItems:64,items:{type:"string",maxLength:4e3}}}}},terminalPayloads:refs=>({complete:{required:["disposition"],properties:{disposition:{type:"string",enum:["full_text_prepared","ocr_required"]}}},failed:{properties:{issue:issueSchema("paper.prepare",refs.inputKind==="generation_text"?"paper.ocr_unreadable":refs.inputKind==="source_text"?"paper.source_incomplete":"paper.prepare_failed")}},blocked:{properties:{issue:issueSchema("paper.prepare","paper.prepare_blocked")}}}),complete:(receipt,context)=>{const disposition=receipt.terminal.disposition;const selected=context.inputKind==="generation_text"?context.input:context.normalized;const allowed=new Set(context.inputKind==="generation_text"?[context.input]:[context.normalized]);const artifactsAreBound=receipt.artifacts.every(artifact=>allowed.has(artifact.path));if(disposition==="full_text_prepared")return receipt.selected_input===selected&&artifactsAreBound&&receipt.artifacts.some(artifact=>artifact.role==="normalized_text"&&artifact.path===selected&&artifact.exists===true&&artifact.usable===true);return disposition==="ocr_required"&&context.inputKind==="source_pdf"&&receipt.selected_input===null&&artifactsAreBound&&receipt.artifacts.some(artifact=>artifact.role==="normalized_text"&&artifact.path===context.normalized&&artifact.exists===true&&artifact.usable===false)&&receipt.artifacts.every(artifact=>artifact.usable!==true)},envelope:({materialKey},refs)=>({schema_version:"quasi.stage.request/0.2",operation:"paper.prepare",stage:"Prepare",material_key:materialKey,effect:"writer",objective:refs.inputKind==="generation_text"?"Semantically verify the exact committed Paper OCR generation text.":"Extract and semantically verify readable text from the exact accepted Paper source.",source:refs.sourceCandidate,input:{role:refs.inputKind,path:refs.input},refs:{normalized:refs.normalized,legacy_recovery_source:refs.legacyRecoverySource,legacy_recovery_text:refs.legacyRecoveryText},capabilities:refs.inputKind==="generation_text"?["Read only the exact generation_text input named by this request; do not read or compare the fixed normalized or legacy recovery refs."]:[`quasi-extract text ${posixSingleQuote(refs.input)} ${posixSingleQuote(refs.normalized)} --json`,"Read the exact input and normalized text artifacts named by this request"],legacy_recovery_rule:"The fixed legacy recovery refs are read-only evidence. Never write, replace, delete, rename, link, or select them.",disposition_contract:{full_text_prepared:"Return only after selected_input was actually read and contains the complete substantive Paper, not a landing page, abstract, preview, or metadata shell.",ocr_required:"Allowed only for a direct PDF input whose extracted normalized text exists but is semantically unusable."},artifact_roles:["normalized_text"]})},makeOcrGenerationRow({operation:"paper.ocr",kind:"paper",validationPolicy:"paper-text-v1"}),{operation:"paper.analyse",context:(rawContext,base)=>({...base,input:rawContext.input}),refs:({input,output,mode})=>({input,output,mode}),writeTargets:({output})=>[{scope:"exact",path:output}],payloadProperties:({input,output})=>({required:["input_path","output_path","artifact_roles"],properties:{input_path:{const:input},output_path:{const:output},artifact_roles:{type:"array",minItems:1,maxItems:1,items:{const:"canonical"}}}}),terminalPayloads:actionPayloads,complete:(receipt,context)=>[...context.mode==="create"?["create"]:["repair"],"reconciled"].includes(receipt.terminal.action),envelope:({meta,materialKey,diagnostics},refs)=>({schema_version:"quasi.stage.request/0.2",operation:"paper.analyse",stage:"Analyse",material_key:materialKey,input:{role:"normalized_text",path:refs.input},output:{role:"canonical",path:refs.output},identity:{title:meta.title,authors:meta.authors,year:meta.year,doi:meta.doi||null,journal:meta.journal,confidence:meta.confidence==="verified"?"verified":"provided"},artifact_contract:PAPER_ARTIFACT_CONTRACT,frontmatter_seed:{type:"paper",title:meta.title,authors:meta.authors,year:meta.year,journal:meta.journal,doi:meta.doi||null},mode:refs.mode,overwrite:refs.mode==="repair",repair_diagnostics:refs.mode==="repair"?diagnostics:[]}),promptText:request=>`Execute exactly one paper.analyse operation using this self-contained JSON request.
Do not reinterpret it as another operation and do not read project instruction files.
${JSON.stringify(request,null,2)}`},makeAuditRow({operation:"paper.audit",refs:({target,pass})=>({target,pass}),artifactRoles:["canonical"],targetRole:"canonical",targetScope:"exact",exactPaths:true})];var paperOperations=operationPreparer("paper",[...materialSearchOperationRows,...paperOperationRows]);var prepareOperation3=paperOperations.prepareOperation;var DIGITS=/^[0-9]+$/;var FORBIDDEN_HOST_CHARACTER=/[\u0000-\u0020"#%\/:<>?@[\]^|\\]/;var IPV6_HOST=/^\[[0-9a-f:.]+\]$/;var normalizeWebUrl=value=>{if(typeof value!=="string"||value.length===0||value.length>2048||[...value].some(character=>{const code=character.charCodeAt(0);return code<32||code===127}))return null;const trimmed=value.replace(/^ +| +$/g,"");const schemeSeparator=trimmed.indexOf("://");if(schemeSeparator<1)return null;const scheme=trimmed.slice(0,schemeSeparator).toLowerCase();if(scheme!=="http"&&scheme!=="https")return null;const defaultPort=scheme==="http"?80:443;const rest=trimmed.slice(schemeSeparator+3);const authority=rest.split(/[/?#]/,1)[0];if(authority.includes("@"))return null;let host;let rawPort;if(authority.startsWith("[")){const bracket=authority.indexOf("]");if(bracket<0)return null;host=authority.slice(0,bracket+1).toLowerCase();if(!IPV6_HOST.test(host))return null;rawPort=authority.slice(bracket+1)}else{const colon=authority.indexOf(":");host=(colon<0?authority:authority.slice(0,colon)).toLowerCase();if(host.length===0||FORBIDDEN_HOST_CHARACTER.test(host))return null;rawPort=colon<0?"":authority.slice(colon)}let port="";if(rawPort.length>0){const digits=rawPort.slice(1);if(rawPort[0]!==":"||digits.length>0&&!DIGITS.test(digits))return null;const numeric=digits.length===0?defaultPort:Number(digits);if(numeric>65535)return null;if(numeric!==defaultPort)port=`:${numeric}`}const tail=rest.slice(authority.length);const fragment=tail.indexOf("#");const beforeFragment=fragment<0?tail:tail.slice(0,fragment);const queryStart=beforeFragment.indexOf("?");const path=queryStart<0?beforeFragment:beforeFragment.slice(0,queryStart);const query=queryStart<0?"":beforeFragment.slice(queryStart+1);return`${scheme}://${host}${port}${path.length===0?"/":path}${query.length===0?"":`?${query}`}`};var resultSeed2=state=>({material:{requested:{kind:"paper",slug:state.requestedSlug},canonical:state.runtimeSlug===null?null:{kind:"paper",slug:state.runtimeSlug}}});var resumeSeed2=(input,state)=>{const seed=state.runtimeSlug!==null&&state.identity!==null?state.ownerConfirmation===null?{state:"canonical",material_slug:state.runtimeSlug,identity:state.identity}:{state:"canonical",material_slug:state.runtimeSlug,identity:state.identity,owner_confirmation:state.ownerConfirmation}:input.seed;return{route:{kind:"paper",slug:seed.state==="provisional"?seed.requested_slug:seed.material_slug},seed,options:input.options}};var planIssue2=(code,operation,summary)=>({code,operation,summary,retryable:false,observation_request:null});var receiptIssue2=receipt=>{const issue=receipt.terminal.issue;return{code:issue.code,operation:issue.operation,summary:issue.summary,retryable:issue.retryable,observation_request:null}};var stopForOutcome2=(state,outcome)=>{if(outcome.kind!=="receipt")return blockedMaterialResult(resultSeed2(state),outcome.issue);if(outcome.receipt.terminal.status==="complete")return null;if(outcome.receipt.terminal.status==="needs_input")return blockedMaterialResult(resultSeed2(state),planIssue2("workflow.incoherent_gate",outcome.receipt.operation,"The specialist returned a human gate outside its typed material boundary."));return stoppedMaterialResult(resultSeed2(state),outcome.receipt.terminal.status,receiptIssue2(outcome.receipt))};var dispatch2=(runtime,operation,slug,context)=>dispatchPreparedOperation(runtime,prepareOperation3({operation,slug,context,label:`${slug}:${operation}`}));var completedPaper=(state,sourcePath,selectedInput,canonicalPath)=>completeMaterialResult(resultSeed2(state),[{role:"source",path:sourcePath},{role:"normalized_text",path:selectedInput},{role:"canonical",path:canonicalPath}],null);var auditIsClean=receipt=>receipt.remaining_violations===0&&receipt.escalated.length===0;var auditIsSettled=receipt=>auditIsClean(receipt)&&receipt.mutated_paths.length===0;var auditOwnsTarget=(receipt,target)=>receipt.target_path===target&&receipt.escalated.every(diagnostic=>diagnostic.path===target)&&receipt.mutated_paths.every(path=>path===target);var auditOwnershipBlock=state=>blockedMaterialResult(resultSeed2(state),planIssue2("workflow.owner_ambiguity","paper.audit","Audit evidence targeted an artifact outside this Paper."));var auditUnstableBlock=state=>blockedMaterialResult(resultSeed2(state),planIssue2("workflow.audit_unstable","paper.audit","The final Paper audit was not stable and mutation-free."));var auditPaper=async(runtime,input,state,sourcePath,selectedInput)=>{const slug=state.runtimeSlug;const common={...input.options,meta:state.identity,materialKey:`paper:${slug}`};const firstAudit=await dispatch2(runtime,"paper.audit",slug,{...common,pass:1});const firstStop=stopForOutcome2(state,firstAudit);if(firstStop!==null)return firstStop;const firstReceipt=firstAudit.receipt;const target=firstReceipt.target_path;if(!auditOwnsTarget(firstReceipt,target))return auditOwnershipBlock(state);if(auditIsSettled(firstReceipt))return completedPaper(state,sourcePath,selectedInput,target);if(auditIsClean(firstReceipt)){const stabilityAudit=await dispatch2(runtime,"paper.audit",slug,{...common,pass:2});const stabilityStop=stopForOutcome2(state,stabilityAudit);if(stabilityStop!==null)return stabilityStop;const stabilityReceipt=stabilityAudit.receipt;if(!auditOwnsTarget(stabilityReceipt,target))return auditOwnershipBlock(state);return auditIsSettled(stabilityReceipt)?completedPaper(state,sourcePath,selectedInput,target):auditUnstableBlock(state)}const repaired=await dispatch2(runtime,"paper.analyse",slug,{...common,input:selectedInput,mode:"repair",diagnostics:firstReceipt.escalated});const repairStop=stopForOutcome2(state,repaired);if(repairStop!==null)return repairStop;const secondAudit=await dispatch2(runtime,"paper.audit",slug,{...common,pass:2});const secondStop=stopForOutcome2(state,secondAudit);if(secondStop!==null)return secondStop;const secondReceipt=secondAudit.receipt;if(!auditOwnsTarget(secondReceipt,target))return auditOwnershipBlock(state);if(auditIsSettled(secondReceipt))return completedPaper(state,sourcePath,selectedInput,target);if(auditIsClean(secondReceipt))return auditUnstableBlock(state);return blockedMaterialResult(resultSeed2(state),planIssue2("workflow.repair_exhausted","paper.audit","The bounded Paper repair completed, but the second audit still found violations."))};async function runPaperPlanResult(runtime,input,rememberContinuation){const requestedSlug=input.seed.state==="provisional"?input.seed.requested_slug:input.seed.material_slug;const initialObservation=input.observations.get(observationKey({kind:"paper",slug:requestedSlug}));const state={requestedSlug,runtimeSlug:input.seed.state==="canonical"?input.seed.material_slug:null,identity:input.seed.state==="canonical"?input.seed.identity:null,ownerConfirmation:input.seed.state==="canonical"&&"owner_confirmation"in input.seed?input.seed.owner_confirmation:null,observation:initialObservation};rememberContinuation(resumeSeed2(input,state));const admittedCanonical=input.seed.state==="canonical"&&(paperObservationAdmitsIdentity(initialObservation,input.seed.identity)||"owner_confirmation"in input.seed&&paperObservationAdmitsOwnerContinuation(initialObservation,input.seed));let canonicalReady=admittedCanonical;if(!admittedCanonical){const searchKey=`paper:${requestedSlug}`;const matchedDecision=input.userDecision?.material_key===searchKey&&input.userDecision.operation==="material.search";const rawDecision=decisionForOperation(input.userDecision,searchKey,"material.search",false);const identityDecision=matchedDecision?parseIdentityConflictDecisionValue(rawDecision,void 0,"paper"):null;if(matchedDecision&&identityDecision===null)return blockedMaterialResult(resultSeed2(state),planIssue2("workflow.incoherent_gate","material.search","The identity decision does not echo one valid candidate set and selection."));if(identityDecision?.selected_candidate.kind==="book")return completeMaterialResult({material:{requested:{kind:"paper",slug:requestedSlug},canonical:null}},[],identityDecision.selected_candidate);const query=identityDecision?.selected_candidate.identity??(input.seed.state==="provisional"?input.seed.hints:input.seed.identity);const searchSlug=identityDecision?.selected_candidate.kind==="paper"?identityDecision.selected_candidate.identity.slug:requestedSlug;const searched=await dispatch2(runtime,"material.search",searchSlug,{...input.options,materialKey:searchKey,meta:query,query,...identityDecision===null?{}:{identityDecision}});if(searched.kind==="receipt"&&searched.receipt.terminal.status==="needs_input"){const gate=parseIdentityConflictGate(searched.receipt,"paper");if(gate===null)return blockedMaterialResult(resultSeed2(state),planIssue2("workflow.incoherent_gate","material.search","The identity specialist returned an invalid conflict gate."));return needsInputMaterialResult(resultSeed2(state),receiptIssue2(searched.receipt),gate,resumeSeed2(input,state))}if(searched.kind==="receipt"&&searched.receipt.terminal.status==="failed"){const terminal=searched.receipt.terminal;if(terminal.issue.code==="material.webpage_redirect"){const url=normalizeWebUrl(terminal.webpage_url);if(url===null)return blockedMaterialResult(resultSeed2(state),planIssue2("workflow.incoherent_complete","material.search","The Webpage redirect omitted one valid public HTTP(S) URL."));return completeMaterialResult({material:{requested:{kind:"paper",slug:requestedSlug},canonical:null}},[],{kind:"webpage",url})}if(terminal.webpage_url!==null)return blockedMaterialResult(resultSeed2(state),planIssue2("workflow.incoherent_complete","material.search","A non-redirect Search failure included a Webpage URL."))}const searchStop=stopForOutcome2(state,searched);if(searchStop!==null)return searchStop;const receipt=searched.receipt;const search=receipt.terminal;state.identity=search.identity;const runtimeSlug=search.owner_slug===null?state.identity.slug:search.owner_slug;state.runtimeSlug=runtimeSlug;state.ownerConfirmation=search.owner_slug!==null&&search.owner_slug!==state.identity.slug?{operation:"material.search",identity_slug:state.identity.slug,owner_slug:search.owner_slug}:null;state.observation=input.observations.get(observationKey({kind:"paper",slug:runtimeSlug}))??null;rememberContinuation(resumeSeed2(input,state));if(search.owner_slug!==null){if(state.observation===null||!state.observation.facts.canonical.present||!state.observation.facts.canonical.usable)return needsObservationMaterialResult(resultSeed2(state),[{kind:"paper",slug:runtimeSlug}],resumeSeed2(input,state));canonicalReady=true}}if(state.observation!==null&&paperObservationAdmitsIdentity(state.observation,state.identity))canonicalReady=true;const slug=state.runtimeSlug;const common={...input.options,meta:state.identity,materialKey:`paper:${slug}`};const usableSourceFacts=(state.observation?.facts.sources??[]).filter(({artifact})=>artifact.usable);let sourceCandidate=usableSourceFacts[0]?.candidate??null;let sourcePath=sourceCandidate?.path??null;if(usableSourceFacts.length>1){const materialKey=`paper:${slug}`;const candidates=usableSourceFacts.map(({candidate})=>candidate);const candidatesFingerprint=state.observation.facts.source_candidates_fingerprint;const gate={kind:"paper_source",operation:"paper.prepare",material_key:materialKey,question:"Which exact Paper source should Prepare use?",candidates,candidates_fingerprint:candidatesFingerprint};const matchingDecision=input.userDecision?.material_key===materialKey&&input.userDecision.operation==="paper.prepare";const rawDecision=decisionForOperation(input.userDecision,materialKey,"paper.prepare",false);const sourceDecision=matchingDecision?parsePaperSourceDecisionValue(rawDecision):null;if(matchingDecision&&sourceDecision===null)return blockedMaterialResult(resultSeed2(state),planIssue2("workflow.incoherent_gate","paper.prepare","The Paper source decision does not bind one candidate fingerprint and source path."));const selectedCandidate=sourceDecision?.candidates_fingerprint===candidatesFingerprint?candidates.find(candidate=>candidate.path===sourceDecision.source_path)??null:null;if(selectedCandidate===null)return needsInputMaterialResult(resultSeed2(state),planIssue2("paper.source_selection_required","paper.prepare","Two exact Paper source alternatives are usable and require one bound selection."),gate,resumeSeed2(input,state));sourceCandidate=selectedCandidate;sourcePath=selectedCandidate.path}if(sourcePath===null){const acquired=await dispatch2(runtime,"paper.acquire",slug,common);const acquireStop=stopForOutcome2(state,acquired);if(acquireStop!==null)return acquireStop;return needsObservationMaterialResult(resultSeed2(state),[{kind:"paper",slug}],resumeSeed2(input,state))}const pdfSource=`sources/${slug}.pdf`;const ocrGeneration=sourcePath===pdfSource?state.observation?.facts.ocr_generation??null:null;if(sourcePath===pdfSource&&ocrGeneration===null)return blockedMaterialResult(resultSeed2(state),planIssue2("paper.ocr_status_missing","paper.ocr","The accepted Paper PDF has no current expected OCR generation status."));if(ocrGeneration!==null&&["invalid","unknown"].includes(ocrGeneration.state))return blockedMaterialResult(resultSeed2(state),planIssue2(ocrGeneration.failure??"paper.ocr_generation_unavailable","paper.ocr",`The current expected Paper OCR generation is ${ocrGeneration.state} and cannot be advanced safely.`));if(ocrGeneration?.state==="in_progress"){const ocr=await dispatch2(runtime,"paper.ocr",slug,{...common,ocrGeneration});const ocrStop=stopForOutcome2(state,ocr);if(ocrStop!==null)return ocrStop;return needsObservationMaterialResult(resultSeed2(state),[{kind:"paper",slug}],resumeSeed2(input,state))}const prepared=await dispatch2(runtime,"paper.prepare",slug,{...common,source:sourcePath,sourceCandidate,...ocrGeneration?.state==="committed"?{input:ocrGeneration.paths.text,generationKey:ocrGeneration.generation_key}:{input:sourcePath}});const prepareStop=stopForOutcome2(state,prepared);if(prepareStop!==null)return prepareStop;const prepareReceipt=prepared.receipt;const disposition=prepareReceipt.terminal.disposition;if(disposition==="ocr_required"){if(ocrGeneration?.state!=="missing")return blockedMaterialResult(resultSeed2(state),planIssue2("workflow.incoherent_complete","paper.prepare","Paper Prepare requested OCR without one missing current expected generation."));const ocr=await dispatch2(runtime,"paper.ocr",slug,{...common,ocrGeneration});const ocrStop=stopForOutcome2(state,ocr);if(ocrStop!==null)return ocrStop;return needsObservationMaterialResult(resultSeed2(state),[{kind:"paper",slug}],resumeSeed2(input,state))}const selectedInput=prepareReceipt.selected_input;if(!canonicalReady){const analysed=await dispatch2(runtime,"paper.analyse",slug,{...common,input:selectedInput,mode:state.observation?.facts.canonical.present?"repair":"create"});const analyseStop=stopForOutcome2(state,analysed);if(analyseStop!==null)return analyseStop}return auditPaper(runtime,input,state,sourcePath,selectedInput)}async function runPaperPlanForComposition(runtime,input){let continuation=null;const result=await runPaperPlanResult(runtime,input,current=>{continuation=current});return{result,continuation}}var resultSeed3=seed=>({material:{requested:{kind:"author",slug:seed.slug},canonical:{kind:"author",slug:seed.slug}}});var planIssue3=(code,operation,summary)=>({code,operation,summary,retryable:false,observation_request:null});var receiptIssue3=receipt=>{const issue=receipt.terminal.issue;return{code:issue.code,operation:issue.operation,summary:issue.summary,retryable:issue.retryable,observation_request:null}};var stopForOutcome3=(seed,outcome)=>{if(outcome.kind!=="receipt")return blockedMaterialResult(resultSeed3(seed),outcome.issue);if(outcome.receipt.terminal.status==="complete")return null;if(outcome.receipt.terminal.status==="needs_input")return blockedMaterialResult(resultSeed3(seed),planIssue3("workflow.incoherent_gate",outcome.receipt.operation,"An Author specialist returned a gate outside its typed boundary."));return stoppedMaterialResult(resultSeed3(seed),outcome.receipt.terminal.status,receiptIssue3(outcome.receipt))};var dispatch3=(runtime,operation,slug,context)=>dispatchPreparedOperation(runtime,prepareOperation({operation,slug,context,label:`${slug}:${operation}`}));var uniqueRoutes=members=>{const seen=new Set;const routes=[];for(const member of members){const route=member.leaf.route;const key=observationKey(route);if(seen.has(key))continue;seen.add(key);routes.push(route)}return routes};var resumeSeed3=(seed,options,members,decisionMember)=>({kind:"author",seed,options,members,decision_member:decisionMember});var discoverAuthor=async(runtime,input)=>{const materialKey=`author:${input.seed.slug}`;const common={materialKey,fullName:input.seed.full_name,topic:input.seed.topic};const books=await dispatch3(runtime,"author.discover-books",input.seed.slug,{...common,count:input.options.maxBooks??5});const bookStop=stopForOutcome3(input.seed,books);if(bookStop!==null)return bookStop;const papers=await dispatch3(runtime,"author.discover-papers",input.seed.slug,{...common,count:input.options.maxPapers??10});const paperStop=stopForOutcome3(input.seed,papers);if(paperStop!==null)return paperStop;const candidates=[...books.receipt.candidates,...papers.receipt.candidates];const resolved=await dispatch3(runtime,"author.resolve-membership",input.seed.slug,{...common,candidates});const resolveStop=stopForOutcome3(input.seed,resolved);if(resolveStop!==null)return resolveStop;const rows=resolved.receipt.resolved;const members=[];const seen=new Set;for(const[index,candidate]of candidates.entries()){const row=rows[index];const kind=candidate.kind;const{kind:_transportKind,...identityValue}=candidate;const identity=kind==="book"?parseBookIdentity(identityValue):parsePaperIdentity(identityValue);if(identity===null)return blockedMaterialResult(resultSeed3(input.seed),planIssue3("workflow.incoherent_complete","author.resolve-membership","Author discovery returned an invalid material identity."));const route={kind,slug:row.vault_slug??row.requested_slug};const key=observationKey(route);if(seen.has(key))continue;seen.add(key);members.push({member_route:route,leaf:{route,seed:{state:"canonical",material_slug:route.slug,identity},options:{}}})}if(members.length===0)return blockedMaterialResult(resultSeed3(input.seed),planIssue3("author.no_representative_works","author.resolve-membership","No representative works were found for this Author."));const continuation=resumeSeed3(input.seed,input.options,members,null);return needsObservationMaterialResult(resultSeed3(input.seed),uniqueRoutes(members),continuation)};var coherentContinuation=(continuation,kind)=>continuation.route.kind===kind&&continuation.seed.state==="canonical"&&continuation.seed.material_slug===continuation.route.slug;var representativeArtifact=(result,continuation)=>{const canonical=result.material.canonical;if(canonical===null||canonical.kind!==continuation.route.kind||canonical.slug!==continuation.route.slug)return null;const role=canonical.kind==="paper"?"canonical":"overview";const path=canonical.kind==="paper"?`vault/papers/${canonical.slug}.md`:`vault/books/${canonical.slug}/00-overview.md`;const matches=result.artifacts.filter(artifact=>artifact.role===role&&artifact.path===path);return matches.length===1?matches[0]:null};var stoppedChild=(seed,result)=>stoppedMaterialResult(resultSeed3(seed),result.terminal,result.issue);var runChild=async(runtime,input,member,decision)=>member.leaf.route.kind==="paper"?runPaperPlanForComposition(runtime,childRunInput(input,member,decision)):runBookPlanForComposition(runtime,childRunInput(input,member,decision));var composeChildren=async(runtime,input)=>{const members=input.resumeSeed.members.map(member=>({member_route:member.member_route,leaf:member.leaf}));let decisionMember=input.resumeSeed.decision_member;const completed=new Map;for(let index=0;index<members.length;index+=1){const member=members[index];while(true){if(completed.has(observationKey(member.leaf.route)))break;const getsDecision=decisionMember!==null&&sameClosedValue(member.member_route,decisionMember);const outcome=await runChild(runtime,input,member,getsDecision?input.userDecision:null);if(getsDecision)decisionMember=null;if(!coherentContinuation(outcome.continuation,member.leaf.route.kind))return blockedMaterialResult(resultSeed3(input.resumeSeed.seed),planIssue3("workflow.incoherent_complete",null,"A child Workflow returned an incoherent continuation."));member.leaf=outcome.continuation;const result=outcome.result;if(result.terminal==="needs_observation"){const continuation=resumeSeed3(input.resumeSeed.seed,input.resumeSeed.options,members,decisionMember);return needsObservationMaterialResult(resultSeed3(input.resumeSeed.seed),uniqueRoutes(members),continuation)}if(result.terminal==="needs_input"){if(!("resume_seed"in result)||!sameClosedValue(result.resume_seed,member.leaf)||!["identity_conflict","book_year","book_structure","paper_source"].includes(result.gate.kind))return blockedMaterialResult(resultSeed3(input.resumeSeed.seed),planIssue3("workflow.incoherent_gate",result.issue.operation,"A child gate did not bind its effective continuation."));const continuation=resumeSeed3(input.resumeSeed.seed,input.resumeSeed.options,members,member.member_route);return higherOrderNeedsInputMaterialResult(resultSeed3(input.resumeSeed.seed),result.issue,member.leaf.route,result.gate,uniqueRoutes(members),continuation)}if(result.terminal==="blocked"||result.terminal==="failed")return stoppedChild(input.resumeSeed.seed,result);if(result.terminal!=="complete")return blockedMaterialResult(resultSeed3(input.resumeSeed.seed),planIssue3("workflow.incoherent_complete",null,"A leaf Workflow returned a terminal unavailable to Author."));if(result.next?.kind==="webpage")return blockedMaterialResult(resultSeed3(input.resumeSeed.seed),planIssue3("material.webpage_redirect_unsupported_in_composition","material.search","An Author Paper member resolved to a Webpage; composition does not silently substitute material kinds."));if(result.next?.kind==="book"){const route={kind:"book",slug:result.next.identity.slug};member.leaf={route,seed:{state:"canonical",material_slug:route.slug,identity:result.next.identity},options:{}};if(!input.childObservations.has(observationKey(route))){const continuation=resumeSeed3(input.resumeSeed.seed,input.resumeSeed.options,members,decisionMember);return needsObservationMaterialResult(resultSeed3(input.resumeSeed.seed),uniqueRoutes(members),continuation)}continue}const artifact=representativeArtifact(result,member.leaf);if(artifact===null||member.leaf.seed.state!=="canonical")return blockedMaterialResult(resultSeed3(input.resumeSeed.seed),planIssue3("workflow.incoherent_complete",null,"A completed child did not prove its exact representative artifact."));const key=observationKey(member.leaf.route);if(!completed.has(key))completed.set(key,{material_key:key,kind:member.leaf.route.kind,id:member.leaf.route.slug,path:artifact.path,title:member.leaf.seed.identity.title});break}}return[...completed.values()]};var completeAuthor=seed=>completeMaterialResult(resultSeed3(seed),[{role:"canonical",path:`vault/authors/${seed.slug}.md`}],null);var synthesiseAuthor=async(runtime,input,children)=>{const seed=input.resumeSeed.seed;const common={materialKey:`author:${seed.slug}`,fullName:seed.full_name,topic:seed.topic,inputs:children};const firstSynthesis=await dispatch3(runtime,"author.synthesise",seed.slug,{...common,mode:input.observation.facts.canonical.present?"repair":"create"});const synthesisStop=stopForOutcome3(seed,firstSynthesis);if(synthesisStop!==null)return synthesisStop;const target=`vault/authors/${seed.slug}.md`;const firstAudit=await dispatch3(runtime,"author.audit",seed.slug,{materialKey:common.materialKey,target,pass:1});const firstAuditStop=stopForOutcome3(seed,firstAudit);if(firstAuditStop!==null)return firstAuditStop;const firstReceipt=firstAudit.receipt;if(firstReceipt.remaining_violations===0)return completeAuthor(seed);const repaired=await dispatch3(runtime,"author.synthesise",seed.slug,{...common,mode:"repair",diagnostics:firstReceipt.escalated});const repairStop=stopForOutcome3(seed,repaired);if(repairStop!==null)return repairStop;const secondAudit=await dispatch3(runtime,"author.audit",seed.slug,{materialKey:common.materialKey,target,pass:2});const secondAuditStop=stopForOutcome3(seed,secondAudit);if(secondAuditStop!==null)return secondAuditStop;if(secondAudit.receipt.remaining_violations===0)return completeAuthor(seed);return blockedMaterialResult(resultSeed3(seed),planIssue3("workflow.repair_exhausted","author.audit","The bounded Author repair completed, but the second audit still found violations."))};async function runAuthorPlan(runtime,input){if(input.mode==="discover")return discoverAuthor(runtime,input);const children=await composeChildren(runtime,input);return Array.isArray(children)?synthesiseAuthor(runtime,input,children):children}var materialKind="author";var workflowMeta={name:"Quasi Author",description:"Compose representative Paper and Book workflows for one Author.",phases:[{title:"Search"},{title:"Analyse"},{title:"Synthesise"},{title:"Audit"}]};async function run(runtime,raw){const parsed=parseAuthorRunInput(raw);return parsed.ok?runAuthorPlan(runtime,parsed.value):parsed.result}return __toCommonJS(author_entry_exports);})();

const __quasiArgs = (() => {
  if (typeof args !== "string") return args
  try {
    return JSON.parse(args)
  } catch {
    return args
  }
})()

return await __quasiWorkflow.run({ agent, pipeline }, __quasiArgs)
