{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "quasi",
  "description": "Academic research toolkit — search, download, extract, analyze, synthesize",
  "version": "0.18.0",
  "author": {
    "name": "ramu"
  },
  "homepage": "https://github.com/giraphant/quasi",
  "repository": "https://github.com/giraphant/quasi",
  "keywords": [
    "academic-research",
    "papers",
    "books",
    "knowledge-base",
    "translation"
  ],
  "userConfig": {
    "cookiecloud_server": {
      "type": "string",
      "title": "CookieCloud server URL",
      "description": "Self-hosted CookieCloud endpoint (https://github.com/easychen/CookieCloud), e.g. https://cookiecloud.example.com. Leave empty to disable EZProxy entirely — papers will only resolve via OA / Sci-Hub / Wayback."
    },
    "cookiecloud_uuid": {
      "type": "string",
      "title": "CookieCloud user key (UUID)",
      "description": "The 'user KEY' set in the CookieCloud Chrome extension."
    },
    "cookiecloud_password": {
      "type": "string",
      "title": "CookieCloud E2E password",
      "description": "End-to-end encryption password set in the CookieCloud Chrome extension. Stored in the system keychain.",
      "sensitive": true
    },
    "cookiecloud_ezproxy_domain": {
      "type": "string",
      "title": "EZProxy cookie domain",
      "description": "Domain where the EZProxy auth cookie is set, e.g. '.idm.oclc.org'. Only cookies on this exact domain are kept; tracking cookies on rewritten publisher subdomains are dropped."
    },
    "cookiecloud_login_url": {
      "type": "string",
      "title": "EZProxy login URL prefix",
      "description": "EZProxy login redirector that DOIs are wrapped through. Example: http://ezp-prod1.hul.harvard.edu/login?url= (Harvard) or your institution's equivalent.",
      "default": "http://ezp-prod1.hul.harvard.edu/login?url="
    },
    "anna_donator_key": {
      "type": "string",
      "title": "Anna's Archive donator key",
      "description": "Donator key from https://annas-archive.org/donations — unlocks the Fast Download API used for batch book acquisition. Stored in the system keychain.",
      "sensitive": true
    },
    "anna_mirrors": {
      "type": "string",
      "title": "Anna's Archive mirrors",
      "description": "Mirror URLs to try in order (one per line, or leave default). The first reachable mirror is used.",
      "multiple": true,
      "default": [
        "https://annas-archive.gl",
        "https://annas-archive.pk",
        "https://annas-archive.gd"
      ]
    },
    "immersive_auth_key": {
      "type": "string",
      "title": "Immersive Translate Zotero auth key",
      "description": "Auth key for the Immersive Translate Zotero PDF translation API. Used by the translate skill. Stored in the system keychain.",
      "sensitive": true
    }
  }
}
