{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "quasi",
  "description": "Academic research toolkit — search, download, extract, analyze, synthesize",
  "version": "0.33.1",
  "author": {
    "name": "ramu"
  },
  "homepage": "https://github.com/giraphant/quasi",
  "repository": "https://github.com/giraphant/quasi",
  "keywords": [
    "academic-research",
    "papers",
    "books",
    "knowledge-base",
    "translation"
  ],
  "userConfig": {
    "cookiecloud_server": {
      "type": "string",
      "title": "CookieCloud endpoint",
      "description": "Endpoint of your CookieCloud server used to pull browser cookies for EZProxy downloads, e.g. https://cookiecloud.example.com. Leave empty to disable EZProxy entirely."
    },
    "cookiecloud_uuid": {
      "type": "string",
      "title": "CookieCloud user key (UUID)",
      "description": "The 'user KEY' set in the CookieCloud Chrome extension."
    },
    "cookiecloud_password": {
      "type": "string",
      "title": "CookieCloud E2E password",
      "description": "End-to-end encryption password set in the CookieCloud Chrome extension. Stored in the system keychain.",
      "sensitive": true
    },
    "cookiecloud_ezproxy_domain": {
      "type": "string",
      "title": "EZProxy cookie domain",
      "description": "Cookie domain used by your institution's EZProxy after login, e.g. .idm.oclc.org or ezproxy.example.edu. This is not the login URL; only cookies on this exact domain are kept."
    },
    "cookiecloud_ezproxy_base_url": {
      "type": "string",
      "title": "EZProxy base URL",
      "description": "Base URL of your institution's EZProxy, e.g. https://ezproxy.example.edu. quasi appends /login?url= internally."
    },
    "anna_donator_key": {
      "type": "string",
      "title": "Anna's Archive donator key",
      "description": "Donator key from https://annas-archive.org/donations — unlocks the Fast Download API used for batch book acquisition. Stored in the system keychain.",
      "sensitive": true
    },
    "anna_mirrors": {
      "type": "string",
      "title": "Anna's Archive mirrors",
      "description": "Mirror URLs to try in order (one per line, or leave default). The first reachable mirror is used.",
      "multiple": true,
      "default": [
        "https://annas-archive.gl",
        "https://annas-archive.pk",
        "https://annas-archive.gd"
      ]
    },
    "immersive_auth_key": {
      "type": "string",
      "title": "Immersive Translate Zotero auth key",
      "description": "Auth key for the Immersive Translate Zotero PDF translation API. Used by the translate skill. Stored in the system keychain.",
      "sensitive": true
    },
    "kagi_session_token": {
      "type": "string",
      "title": "Kagi session token",
      "description": "Kagi Session Link URL or raw session token for Douban Chinese-edition discovery. Stored in the system keychain and passed to kagi-cli as KAGI_SESSION_TOKEN.",
      "sensitive": true
    }
  }
}
